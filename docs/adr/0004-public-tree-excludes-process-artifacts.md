# 公开树不含过程物

GitHub 默认 clone 只给玩家和生态作者看源码、给人看的文档、当前正式交付包和发布台账。票、地图、规格、审查链和实机日志留在工作副本。已推送历史不改写。

**Status: accepted**
**Date: 2026-09-20**

公开仓对标成熟模组平台（BepInEx 一类）：工单不在默认树里，二进制走 Releases，构建日志不进库。BUE 当时把 `.scratch/` 和 `audit/<日期>/` 跟源码一起推，默认 clone 里过程物远多于产品代码。

考虑过改写已推送历史、另开干净仓、以及 33 条未推送提交原样出门。改写会弄断台账路径和已有 clone；另开仓要迁身份；原样推等于再公开固化一轮噪音。选定：本地保留过程物；已推送历史不动；未推送的第六阶段出门前把过程物滤出公开 `main`；`audit/RELEASES.md` 仍进公开树；日期证据与 `.scratch/` 不进；V1 已跟踪的 329 个二进制不追溯；旧阶段 `publish/` 包体积小，继续当归档留在树里；当前 DLL 同时走 `publish/` 与 GitHub Release。
