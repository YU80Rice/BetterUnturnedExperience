# Handoff：续作 DEV-V2-05（V1 兼容层）

- 日期：2026-09-03（前一长会话压缩，用此文档跨会话交接）
- 交接方已完成：DEV-V2-01~04（契约冻结、网络运行时、LMN 帧识别）、spec V2 第一阶段、8 张实施票、T1/T5/T6/T8 research 证据链
- 接手任务：**DEV-V2-05 V1 兼容层（Status: claimed）**
- 仓库根定位：本文件在 `.scratch/bue-v2-lmn-adoption/handoffs/`，仓库根向上三级（`docs/`、`src/`、`tests/`、`eng/` 同级）

## 新会话开头语（建议直接粘贴）

> 读 `.scratch/bue-v2-lmn-adoption/handoffs/handoff-DEV-V2-05-20260903.md`，继续 DEV-V2-05。

## 一句话任务

实现 LMN V1 数字频道兼容路径（spec T4 决策），旧插件**不改代码**即可收发；"是否启用 V1 兼容"为官方功能（面板可关）。完整范围/验收条件/不做清单 → **读票** `../issues/DEV-V2-05-v1-compat-layer.md`（内含 2026-09-03 拍板口径节，三条 human 定稿已落盘，无需重新征询）。spec 上下文 → `../spec-V2-phase1-lmn-adoption.md` L71-79（V1 兼容路径决策）、L120（兼容/测试面）。

## 已知地基（勿重复造轮子）

- 帧识别已存在：`src/BetterUnturnedExperience.Core/Network/LmnFrameClassifier.cs`（DEV-V2-04，纯静态 `IsLmnFrame`）。05 新增的是 **MOD 帧内 int 频道号提取/路由 + V1 兼容注册表 + 官方开关**，三者在 04 产物里都没有。
- `LmnTakeoverCoordinator.cs`（04 产物）未实例化接线——拍板已交割到 DEV-V2-06，**本票不做接线**。
- V1 API 判据与行号证据 → `../research/V2-T8-v1-ecosystem-inventory.md`：判据 A 注册/注销 L137-216（`RegisterServerHandler(int,…)` 等）、判据 B 发送 L415-495、判据 C MOD 魔数帧+首字节 0..255 频道号；频道 100/101/102 已迁 V2、103 未发布。**LMN 源码（ModRouter.cs 等）不在本仓库**，T8 research 是权威来源，引用其行号即可。
- V1 兼容边界（spec CONTEXT 词汇）：只为既有消费方保留兼容接收/运行，不提供新 V1 注册入口、不改旧插件。
- V1 帧结构在 Host 内部固化注释（不进 Contracts，Contracts 12 项已冻结）。

## 硬规则（仓库 AGENTS.md 强制）

- **红测先行 TDD**：`--bue-v1-compat-red` 锚点先红后绿。
- **双轴独立审查**（Standards + Spec 并行子代理），BLOCKED→修→重审→CLEAN 才可交付 → `docs/agents/output-review-loop.md`。
- 真机复测 → `docs/agents/real-machine-test-loop.md`；**本票真机 no-op 验证是可选项**，不阻塞闭环，交付报告证据链标注"真机验证待补"即可。
- 门禁矩阵：Release 0 errors / 0 warnings + 七项目测试运行器 exit=0（`tests\<Project>\bin\Release\<Project>.exe`）+ UI token 扫描 `eng\Verify-NoUiTokens.ps1`（禁 BepInEx/Harmony 等词，规避写法用全小写）+ `git diff --check`。
- git 惯例：`src/BetterUnturnedExperience.Contracts/` 与 `src/BetterUnturnedExperience.Core/` 目录曾被 untracked，改动须**显式 git add**；bin/obj 不提交；Editor 保存 CRLF → 提交前转 LF；提交信息 `feat(DEV-V2-05): …`；只 stage 源码+票+交付报告。

## 阶段快照（已冻结）

开工前 manual snapshot 已存（undo-savepoint：20260903-202452-18b2，6 文件）。若中途需要回退，用 undo_list / undo_restore，不要手改配置。

## 后续票（本票 CLEAN 交付后）

DEV-V2-06（接线 + 配置迁移：LmnTakeoverCoordinator 实例化、面板开关、BUE 配置迁移）→ 07 → 08。全部在 `../issues/`。顺序拍板：05 先于 06（05 是独立并行线，前置 04 已解除）。

## Suggested skills

- `implement`：按票实施主体代码（票已 self-contained，含验收断言）。
- `tdd`：红测先行，`--bue-v1-compat-red` 先红后绿。
- `code-review`：交付前双轴审查（并行 Standards/Spec 子代理），CLEAN 后归档交付报告。
- `claude-handoff` / `handoff`：若本次又会写满上下文，再次跨会话接力时使用。
