# DEV-V2-09：主菜单注入按钮间距与原版图标节奏对齐

Type: task
Status: resolved（2026-09-05，用户实机截图视觉验收通过：BUE 按钮与商店位间隙已与原版相邻项节奏一致，0px 贴合消失；候选 DLL 3370F5D8…E007 部署生效。实施链=红0+红1→绿、R1 修复→R2 双 CLEAN，提交 c042bc0；证据 audit/2026-09-05/DEV-V2-09/retest-menu-user-acceptance.png）
Parent: spec-V2-phase1-lmn-adoption（DEV-V2-07 配置 A 实机采集中发现）
Source: DEV-V2-07 配置 A 复核记录 `audit/2026-09-04/DEV-V2-07/configA-verification-r1.md`（用户报告 + 主菜单截图）

## Scope

主菜单左侧栏「BUE 插件管理」按钮与其上方商店位（原版 `SleekItemStoreMainMenuButton`，截图中显示 UMarine Avenger）垂直间隙为 0px（两矩形贴合），而原版左列相邻项的视觉间隙是 10px（节距 60px − 按钮高 50px）。修复：把注入按钮插槽从「贴合商店位底缘」改为「按原版节距取下一槽位」（y 460→470），恢复原版菜单节奏。修复走独立红测先行 + 双轴 CLEAN 后出独立增量，不回灌 07 已授予身份。

## 根因（triage 定位，2026-09-05，已复核）

- 原版 `MenuDashboardUI` 左列全部为 container 直接子级、尺寸 200×50，顶部 y 依次：Play 170 / Survivors 230 / Configuration 290 / Workshop 350 / 商店 410，即节距 60px、相邻视觉间隙 10px；Exit 固定在容器底缘（PositionScale_Y=1）。
- BUE 注入按钮（`BueNativeManagementPanel.TryAddDashboardButton` 经 Glazier 创建）硬编码 `PositionOffset_Y = 460f`，恰等于商店按钮底缘（410+50），与商店位零间隙，比原版节奏紧 10px——即截图报告的不一致，方向为偏紧（非偏大）。
- 证据：对 Libs 快照（2026-08-11）与实机（E:\Steam，2026-09-04）两份 Assembly-CSharp.dll 分别反编译 `MenuDashboardUI`，布局参数完全一致，结论不依赖过期快照。
- 排除项：V1 时代注入器（外层 UnturnedPluginManager）不注入主菜单左列，票面「可能涉及 V1」假设不成立。

## 验收条件

- [x] 红测先行：新增纯 C# 测试断言「BUE 主菜单插槽 Y == 商店槽位(410) + 原版节距(60) = 470」，先红后绿（红0 编译红 + 红1 行为红，布局值从注入路径抽成 headless 可断言的常量/纯类型）。
- [x] 注入路径不再内联布局字面量，改从抽取的布局契约取值；MenuWorkshopUI / PlayerPauseUI 注入按钮行为不变。
- [x] 实机截图前后对照：主菜单一列内「BUE 插件管理」与商店位间隙 == 原版相邻图标项间隙（10px 视觉 / 60px 节距）。（用户 2026-09-05 截图验收通过）
- [x] 不影响其它注入 UI（管理面板/背包面板）布局；现有 ClientUi / Plugin.Tests 全绿。
- [x] 走 output-review-loop：红测先行 + 双轴独立评审 CLEAN 后出独立增量（R2 双 CLEAN，不回灌 07 已授予身份）。
- [x] 实施串行：DEV-V2-12 候选闭环之后才启动（其 resolved + 人工批准 eff5c0d 在先）。

## 不做

- 不改网络/面板功能逻辑；不动 DEV-V2-07 冻结候选（07 源码零修改约束不变）。
- 不动 MenuWorkshopUI（居中入口）与 PlayerPauseUI（暂停页入口）布局——另一布局语境，与左列节奏无关。
- 不做按钮视觉对齐（SleekButtonIcon 图标样式/字体/宽度）——本票只管垂直间距。
- 不 nudge/移动任何原版按钮（Glazier 无公开子枚举，维持既有约束）；亦不做商店按钮存在性探测/自适应（权衡记录见 Agent Brief）。

## Comments

> 2026-09-04 建票（agent）：DEV-V2-07 配置 A 实机采集中用户报告并附截图（主菜单「BUE 插件管理」蓝箭头、商店位「UMarine Avenger」红箭头，间距与原版图标间隔不一致，用户原话「看着膈应，需要调整一下」）。根因假设：注入按钮未复用原版 spacing/margin 常量或被硬编码偏移——待定位。07 源码零修改约束下不在 07 内修，立票后续接。

> *This was generated by AI during triage.*
>
> 2026-09-05 triage（agent）：根因定位完成（460=商店底缘贴合，0px 间隙 vs 原版 10px），票面猜测已更正（注入器在 Plugin 程序集非 ClientUi；V1 排除；偏紧非偏大）。以下为实施契约。

## Agent Brief

**Category:** bug
**Summary:** 主菜单左列 BUE 按钮插槽 y=460 与商店位（410..460）零间隙贴合，破坏原版 60px 节距；改为 y=470 并把布局值抽成 headless 可测契约。

**Current behavior:**
主菜单左列注入按钮顶槽 y=460，与上方商店按钮（y=410、高 50）底缘完全贴合，视觉间隙 0px；原版左列相邻项（Play/Survivors/Configuration/Workshop/商店）节距 60px、视觉间隙 10px。用户实机截图确认节奏不一致（偏紧）。

**Desired behavior:**
BUE 按钮占据原版节距的下一槽位 y=470：商店位存在时（常态）与商店位间隙恰为原版 10px；商店位缺位时上方间隙 70px，属可接受退化。布局值不再内联在注入路径。

**Key interfaces:**
- `BueNativeManagementPanel.TryAddDashboardButton(string)`：当前内联 `PositionOffset_Y = 460f`（另有 Size 200×50、FontSize Medium 不动）；改为引用下方契约。
- 新增 internal 纯 C# 布局契约（常量类或静态属性，命名由实施者定，放 Plugin 程序集与现有 seam 同层）：至少暴露原版节距 60、商店槽位 410、BUE 槽位 = 商店槽位 + 节距（=470），headless 可断言。
- 原版参照（只读）：`MenuDashboardUI` 的 SleekButtonIcon 系列 + `SleekItemStoreMainMenuButton`，全部 200×50、节距 60。
- 红测落点：`BetterUnturnedExperience.Plugin.Tests` 既有自研 Assert 框架，按 coordinator/seam 用例同风格新增断言函数。

**权衡记录（triage 已拍板，勿重新发明）：**
- 商店按钮条件性存在（原版仅在有商店 listing 时创建），且 Glazier 无公开子枚举、无法可靠探测其存在 → BUE 槽位固定 470，不做运行时自适应。
- 反编译复核命令：`ilspycmd Assembly-CSharp.dll -t SDG.Unturned.MenuDashboardUI`（实机 DLL 在 E:\Steam 的 Unturned_Data\Managed）。注意仓库 Libs 快照（2026-08-11）落后实机（2026-09-04），本次两版参数一致；后续票如再引用原版布局，须对实机 DLL 复核。

**Acceptance criteria:**
- [ ] 红测先行：断言 BUE 槽位 Y == 410 + 60 = 470 的测试先红（对当前 460）后绿。
- [ ] 注入路径取值自抽取契约，MenuWorkshopUI / PlayerPauseUI 注入不受影响。
- [ ] 现有 Plugin.Tests 全绿；ClientUi 测试全绿。
- [ ] 实机截图前后对照：与商店位间隙 == 原版相邻项间隙。
- [ ] 双轴独立评审 CLEAN 后出独立增量，不回灌 07 已授予身份。

**Out of scope:**
- MenuWorkshopUI / PlayerPauseUI 注入按钮布局调整。
- 按钮视觉对齐（图标/字体/宽度/文案）。
- 商店按钮存在性探测与槽位自适应。
- 任何原版按钮的移动或 nudging。
- 与 DEV-V2-12 并行的任何 Plugin 源码改动（实施串行到其候选闭环之后）。

## 实施结单（2026-09-05，agent /implement）

- **红绿链**：红0 编译红（CS0103×8，`audit/2026-09-05/DEV-V2-09/red0-compile.log`）→ 红1 行为红（契约节距临时 50f = bug 形状 460，断言咬住后复原，`red1-behavioral.log`）→ 绿（解决方案 Release 重建 0 警告 0 错误 + 七套测试全 PASS，`green-*.log` ×8 落盘）。
- **双轴**：R1 Standards CLEAN / Spec FINDINGS（F1 红证不足、F3 绿证未落盘→已修复；F2 实机截图、F4 流程进行时→具名延期）→ R2 双 CLEAN。链路详见 `audit/2026-09-05/DEV-V2-09/DEV-V2-09-closing-report.md`。
- **变更**：新增 `BueMenuEntryLayout.cs` 布局契约（X=0、200×50、节距 60、商店槽 410、BUE 槽 470）入 Plugin csproj；`TryAddDashboardButton` 改从契约取值并更正陈旧注释；Plugin.Tests 新增六断言布局测试。
- **候选身份**：`BetterUnturnedExperience.dll` SHA-256 `3370f5d8106b4121b635f584b83ceffae971456cfa6040ae886db9ffb2e74007`（270848 字节，两轮重建逐字节一致）。不继承 07/12 发布批准。

## 人工实机复测清单（停等中）

1. 部署：把 `src/BetterUnturnedExperience.Plugin/bin/Release/BetterUnturnedExperience.dll` 复制到实机 `E:\Steam\steamapps\common\Unturned\BepInEx\plugins\BetterUnturnedExperience.dll`（多端按手册各端都换）。
2. 指纹核对：启动后日志 `BUE-MANAGEMENT-TRACE-002 event=assembly-identity sha256=3370F5D8…` 与候选一致。
3. 截图对照：主菜单左列「BUE 插件管理」与上方商店位（UMarine Avenger）之间出现目测与原版相邻项一致的间隙（原版节奏=10px 视觉 / 60px 节距），修复前 0px 贴合。
4. 回归确认：面板打开/关闭正常，管理面板与背包面板布局无异常。
5. 对照通过 → 本票置 resolved；不通过 → 回炉。

## 复测记录（2026-09-05）

用户部署候选 DLL（3370F5D8…E007）后回传主菜单截图（归档 `audit/2026-09-05/DEV-V2-09/retest-menu-user-acceptance.png`）：左列节奏 开始游戏→角色设定→游戏设置→创意工坊→商店位（Knitted Sweater Maplestrike ¥7.00）→「BUE 插件管理」，BUE 按钮与商店位间隙与原版相邻项一致，0px 贴合消失。用户原话「视觉验收没问题」→ 验收清单 5/5 达成，票 resolved。
