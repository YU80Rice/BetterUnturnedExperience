# V2 第一阶段实施阶段快照 — DEV-V2-04 开工前（2026-09-03）

> 性质：V2 第一阶段实施过程快照（DEV-V2-03 完成 → DEV-V2-04 开工前）。
> 本快照非发布/Stable 授权；三环境验证（DEV-V2-07）完成前不宣称运行或发布通过。

## 1. 已完成

| 项 | 提交 | 内容 |
|---|---|---|
| V2 规划冻结 | `9ecac9f` | wayfinder 7/7 + spec 批准 + 8 张实施票 |
| DEV-V2-01 SDK 基线 | `dc309ca` | SDG.NetTransport 刷新 + ITransportConnection 清单固化 |
| DEV-V2-02 契约类型 | `4a37d4b` | `BueNetwork` 命名空间 5 类型 + Contracts 源码入库 |
| DEV-V2-03 运行时 | `c9027b7` | BueNetworkRuntime（频道路由/Hello-Ack 握手/会话/双向收发），双轴 R3 CLEAN |

## 2. 实施工单依赖链（当前 frontier）

```
DEV-V2-01 ✅ ──┐
               ├──→ 03 ✅ BueNetworkApi 运行时 ──→ 04 LMN 接管 ⏳（本会话认领）──┬──→ 05 V1 兼容层（需人工实机）
DEV-V2-02 ✅ ──┘                                                  │            └──→ 06 空迁移+网络设置
                                                                  └──→ 07 三环境验证（需人工实机）──→ 08 生态迁移验证（需人工实机）
```

## 3. DEV-V2-04 边界（T5 决策落地）

- 检测：`Chainloader.PluginInfos.ContainsKey("com.yu80rice.launchmultiplayernet")` 只读枚举已加载实例；检测时机 = 网络模块初始化时；只处理"已加载"口径。
- 停用：BUE 网络模块对 `NetMessages.ReceiveMessageFromClient/Server` 注册 `Priority.First` Prefix，命中 MOD/LMN2 帧 `return false` 短路；未命中 `return true` 放行。
- 红线：不引入 `[BepInIncompatibility(LMN_GUID)]`（连坐 LIT/LIR/LHT）。
- 残响：接受 LMN Awake 仍执行、路由被短路（无业务副作用）。
- 面板：管理面板显示「已由 BUE 接管」状态条目 + 「让我改回独立 LMN」按钮（可逆性展示）。
- 移交范围（DEV-V2-03 Spec R3 建议）：可靠透传（wire reliability bit）+ 按帧来源解析分发会话。

## 4. 人工实机节点

- DEV-V2-05（V1 兼容层旧插件实机）/ 07（三环境）/ 08（LIT/LIR/LHT）——届时提醒用户。
