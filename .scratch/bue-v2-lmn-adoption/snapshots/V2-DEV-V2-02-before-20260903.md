# V2 第一阶段实施阶段快照 — DEV-V2-02 开工前（2026-09-03）

> 性质：V2 第一阶段实施过程快照（DEV-V2-01 完成 → DEV-V2-02 开工前）。
> 本快照非发布/Stable 授权；三环境验证（DEV-V2-07）完成前不宣称运行或发布通过。

## 1. 已完成

| 项 | 提交 | 内容 |
|---|---|---|
| V2 规划冻结 | `9ecac9f` + 票 | wayfinder 7/7 + spec 批准 + 8 张实施票拆分 |
| **DEV-V2-01 SDK 网络基线** | `dc309ca` | `Libs\SDG.NetTransport.dll` 刷新到游戏安装版（`D512DB03...`）+ `ITransportConnection` 成员清单固化 + `--sdk-net-baseline-red` 红测（Send 签名绑定）+ 双轴 CLEAN |

## 2. 实施工单依赖链（当前 frontier）

```
DEV-V2-01 SDK 基线 ✅ ──┐
                        ├──→ 03 BueNetworkApi 运行时 ──→ 04 LMN 接管 ──┬──→ 05 V1 兼容层（需人工实机）
DEV-V2-02 契约类型 ⏳ ──┘                                   │         └──→ 06 空迁移+网络设置
                                                          └──→ 07 三环境验证（需人工实机）──→ 08 生态迁移验证（需人工实机）
```

- frontier（开工时）：DEV-V2-02（契约类型，无阻塞，本会话认领）。
- 人工实机节点：DEV-V2-05 / 07 / 08（届时提醒用户）。

## 3. DEV-V2-02 边界（T3 Q1-Q12 冻结形状）

- `BueNetwork` 命名空间，纯 .NET 4.7.2 / C# 10；不引用 Unity/Glazier/Sleek/LMN/Unturned/BepInEx 具体类型。
- 频道 = FeatureId（一个模块一个命名频道）；注册声明 `MinimumBueContract` + 功能版本。
- 版本协商 API 内部自动，不匹配 `ContractIncompatible`（复用 `FeatureRegistrationReason`）。
- `IConnectionSession`：SessionId（连接代际）+ Connected/Disconnected/GenerationChanged + Send + PeerSteamId + PeerFeatureVersion + Channels。
- 发送目标：SendToServer / SendToClients / SendToClient(session)；无对端 FeatureId 寻址。
- `NetworkSendResult` 显式枚举；`FeaturePresentationState`（SCR-GPT18-001 已批准）一并写入。
- 帧编解码 / IClientTransport 反射 / Steam 细节留 Host 内部（DEV-V2-03），不进 Contracts。
