# V2 第一阶段实施阶段快照 — DEV-V2-03 开工前（2026-09-03）

> 性质：V2 第一阶段实施过程快照（DEV-V2-02 完成 → DEV-V2-03 开工前）。
> 本快照非发布/Stable 授权；三环境验证（DEV-V2-07）完成前不宣称运行或发布通过。

## 1. 已完成

| 项 | 提交 | 内容 |
|---|---|---|
| V2 规划冻结 | `9ecac9f` | wayfinder 7/7 + spec 批准 + 8 张实施票 |
| DEV-V2-01 SDK 网络基线 | `dc309ca` | SDG.NetTransport 刷新 + ITransportConnection 清单固化 + 红测 |
| DEV-V2-02 契约类型 | `4a37d4b` | `BueNetwork` 命名空间 5 类型（T3 Q1-Q12）+ Contracts 源码首次入库 + 红测 |

## 2. 实施工单依赖链（当前 frontier）

```
DEV-V2-01 ✅ ──┐
               ├──→ 03 BueNetworkApi 运行时 ⏳（本会话认领）──→ 04 LMN 接管 ──┬──→ 05 V1 兼容层（需人工实机）
DEV-V2-02 ✅ ──┘                                              │            └──→ 06 空迁移+网络设置
                                                             └──→ 07 三环境验证（需人工实机）──→ 08 生态迁移验证（需人工实机）
```

## 3. DEV-V2-03 边界（T3 契约 + 网络层基础）

- 运行时在 BUE Host 内部（`Core\Network\`，经 Plugin `<Compile Include>` 嵌入），**纯 C# 可测**；实现 `IBueNetworkApi`（DEV-V2-02 契约）。
- 频道注册/版本协商/会话事件/双向链路抽象（`INetworkTransport` 之上；`IClientTransport`/`ITransportConnection` 真实接线属 DEV-V2-04）。
- 帧格式内部（魔数 + 频道 GUID + 载荷），不进 Contracts。
- 不做 V1 兼容（05）、不做 LMN 接管（04）、不暴露帧进 Contracts。
- 旧 `BueEnvelopeCodec`/`BueFrameCodec`（DEV-06 能力协商/设置围栏）为独立协议栈，本票不触碰。
