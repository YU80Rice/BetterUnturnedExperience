# 04: UPM 分类导航与列表配置的文本路径

Type: task
Status: resolved
Category: enhancement
Blocked by: 无（可立即开始；**禁止 merge GitHub PR #1**）
Parent: [map.md](../map.md)
Source: GitHub PR #1（35117 / `d784d7f`，本地分支 `pr-1`）仅作补丁参考

> *This was generated by AI during triage.*

## What to build

外部插件配置若带 `Unturned.Category:*` 或 ItemList / BlueprintList / CreatureList 标签，BUE 面板要能分类浏览，并用**文本路径**编辑列表值。编辑进未保存草稿，保存才写 ConfigEntry。不要把 PR #1 直接合进当前 main。

## Agent Brief

**类别：** enhancement  
**摘要：** 在现行草稿面板上补 Category 导航与三类列表的文本编辑；Cycle 与描述行视为已交付，不得再按第三阶段即时写入模型重做。

**当前行为：**
DEV-V4-02/08 已交付：描述截断 120、Cycle（可写回才画）、草稿保存、失败三类短中文。Category 导航与 Item/Blueprint/Creature **选择器**在 Phase-4 规格里是后续可视化。PR #1 基于 `f03d98a`（无草稿），会与 `ManagementPanel` / 原生面板 / catalog adapter 冲突；它还把 Cycle/描述再做一遍，且列表仍是立刻写盘的文本框。

**期望行为：**
- 解析 `Unturned.Category:<名>`；无标签时按配置节分组。多分类时详情有分类导航（chips 或等价），单分类不画多余导航。
- ItemList / BlueprintList / CreatureList：值仍是字符串，显示格式提示（物品 `ID, ID...` / 配方 `物品ID:配方编号` / 生物 `Z:类型` 或 `A:资产ID`），控件是文本框进草稿，不是 UPM 那种带图标搜索的 picker。
- 未识别列表标签不得整行变只读（若底层仍是字符串）。
- 保存、脏判定、RequiresRestart 徽章、失败文案沿用 Phase-4 草稿纪律。
- 可从 `pr-1` 摘解析函数与测试意图，必须接到当前 `PluginConfigEntryView` 与草稿命令缝；禁止 checkout PR 后 merge。

**关键接口：**
- 外部配置条目的分类与控件种类（Field / Toggle / Cycle / List）
- 草稿提交仍经现有外部 ConfigEntry 保存顺序
- 分类集合：去重、首现序、空分类归「通用」

**验收标准：**
- [x] 红测先行：分类去重与默认组；列表行有格式提示且改值进草稿不立刻 Save；保存后权威更新（双层编译红实录 red-clientui/red-plugin-compile.log → 绿）
- [x] 既有 Cycle / 描述 / 草稿回归保持绿（含 DEV-V4-08 可写回规则；全套 7 exe 0 警告 0 错误）
- [x] 不引入可视化 picker、不扩 2.1 契约、不授候选（Contracts 零改动；RELEASES 不动）
- [x] 全套测试 0 警告 0 错误；双轴 CLEAN（R1 两轴全新实例均 CLEAN，2 条可递延气味具名见审计）
- [x] GitHub PR #1 保持不合并（`d784d7f` 非 main 祖先实证）；PR 上维护者留言非本票强制，由维护者自领

**不在范围内：**
- merge `pr-1`
- UPM 可视化选择器（图标/搜索/懒加载）——第五阶段
- 程序集路径、配置文件路径
- 重做 Cycle 或描述行

## Comments

- 2026-09-13 分类：PR 作者 35117；mergeable 相对旧 base。Cycle/描述 = 已实现，不写入 `.out-of-scope/`。
- 2026-09-13 resolved（实现 `633a8be`）：Category 解析（trim/空名回落配置节）+ 模型分类集合（去重、首现序、空/空白归「通用」，`GetPluginConfigCategories`）与按类过滤投影（`GetPluginConfigRows(stableId, category)`，草稿生效值照常）；ItemList/BlueprintList/CreatureList（裸+冒号两式、固定 Item>Blueprint>Creature 序）=带逐字格式提示的字符串文本行进草稿，列表标签压过 Cycle 标签与 AcceptableValueList；未识别列表标签底层字符串不整行只读；保存/直写重建保留分类/提示/描述/档位。原生面板多分类才画 chips、单分类平铺、换插件复位。红测先行双层编译红实录→全套 7/7 绿；双轴 R1 全 CLEAN。审计 `audit/2026-09-13/POST-P4-04/review-loop.md`。
