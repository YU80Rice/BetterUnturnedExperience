# 02: Plugin.Tests 的 log fixture 未被 git 跟踪

Type: task
Status: resolved
Category: bug
Blocked by: 无（可立即开始）
Parent: [map.md](../map.md)

> *This was generated by AI during triage.*

## What to build

干净克隆能编过 Plugin.Tests。工程引用的两份预览日志 fixture 必须进版本库（或改成不被 `*.log` 忽略的后缀），Clone 下来 CopyToOutput 不再缺文件。

## Agent Brief

**类别：** bug  
**摘要：** 让 Plugin.Tests 引用的两份 `dev16d-*.log` fixture 在干净克隆上可用。

**当前行为：**
全局忽略 `*.log`。Plugin.Tests 仍 `None Include` 两份 Fixtures 下的 log，并 CopyToOutput。工作区磁盘上有文件，但 `git check-ignore` 命中，未跟踪。GitHub PR #1 作者在干净克隆上因此编不过。Phase-4 之前即存在。

**期望行为：**
- 新 clone 含这两份 fixture 的真实内容，构建 CopyToOutput 成功。
- 要么 `git add -f` 跟踪现有 `.log`，要么改后缀（例如 `.log.txt` / `.fixture`）并改工程引用；**二选一，优先改后缀**，避免以后 `git add` 再吞审计日志。
- 其它 `*.log` 忽略规则保持，审计日志仍不入库。

**关键接口：**
- Plugin.Tests 工程里 CopyToOutput 的两份 fixture 项
- 仓库忽略规则与「测试夹具必须入库」的边界

**验收标准：**
- [x] 干净工作树（或 `git check-ignore`）不再把这两份夹具当成忽略且缺失
- [x] Plugin.Tests 构建能解析并复制这两份文件
- [x] 全套测试 7/7；不把无关 `.log` 扫进提交
- [x] 双轴 CLEAN 或本票纯工程卫生经 Standards 轴即可（无契约面）

**不在范围内：**
- 改预览/无预览测试语义
- 放宽全局 `*.log` 忽略
- 发布候选

## Comments

- 2026-09-13 分类：GitHub PR #1 已知边界第 2 条；本地 `git check-ignore` 已确认。
- 2026-09-13 实施（agent）：按票选「优先改后缀」落地。审计链 `audit/2026-09-13/POST-P4-02/review-loop.md`；修复提交 `32c88e6`；双轴 R1 全 CLEAN（Standards 3 条理论缺口具名递延）。终验顺带抓出范围外既有缺口 → 另立票 [08](08-test-runner-host-dlls.md)。

## Answer

两份夹具改后缀 `dev16d-{fixed-preview,r36-no-preview}.log.txt` 入库（`*.log` 忽略规则原样零放宽，`git add -f` 路线按票弃用），`Plugin.Tests.csproj` 两条 `None Include` 与 `Program.cs:501/513` 同步，测试语义零改动。新增通用防回归门禁 `eng/Verify-TestFixturesTracked.ps1`（全部 csproj 的 CopyToOutput `None` 项：盘上存在 + 未被忽略 + 已跟踪；修复前 4 violations 实红，修复后 PASS）。红-1=worktree 干净克隆从旧 HEAD 复现 `MSB3030×2`（PR #1 作者失败原样）；绿=全套 Rebuild 0/0、7 exe 全绿；终验=新 HEAD `32c88e6` 干净克隆全解构建 exit 0、夹具随克隆在库并复制到输出、门禁在克隆 PASS、7 exe 在克隆 7/7（运行器宿主 DLL provisioning 属既有范围外缺口，另票 08）。`.gitignore` 只加两行边界注释。契约仍 2.1，非发布票不授身份。
