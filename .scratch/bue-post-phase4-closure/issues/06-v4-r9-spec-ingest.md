# 06: 将 V4-R9 实时注入裁决并入 Phase-4 规格正文

Type: task
Status: resolved
Category: enhancement
Blocked by: 无（可立即开始）
Parent: [map.md](../map.md)

> *This was generated by AI during triage.*

## What to build

Phase-4 `spec.md` 写明整理按钮九态门时，必须包含关单后增补的 V4-R9：Running 时经宿主时钟泵对**已打开**的背包做幂等补注入；全页在册则静默不再打注入完成日志。代码已在 v5，本票只改规格与地图指针，避免下一阶段按旧「仅 ctor 注入」实施。

## Agent Brief

**类别：** enhancement（文档）  
**摘要：** 把地图上的 V4-R9 与静默短路写进 `spec.md` 冻结正文，不改产品行为。

**当前行为：**
规格仍按开图时的 Q55：停用拆除、Running 才注入。实机证明只靠 ctor 无法在停用后再启用时复装。用户裁定实时注入；后又加全页在册静默短路。地图有增补，规格未并入。

**期望行为：**
- spec LIT 节写清：ctor 覆盖新仪表盘；Running 时时钟泵节流幂等补注入存活仪表盘；非 Running 不新增；拆除失败页保留引用、下次拆除后重试；全页在册补注入零日志。
- 地图 Decisions 指向 spec 对应段。不重开九态门。
- 零代码，或仅注释/测试名与 spec 用语对齐。

**关键接口：**
- Phase-4 spec LIT 标题栏节
- 地图 V4-R9 指针

**验收标准：**
- [x] spec 正文含 V4-R9 与静默短路，与 v5 行为一致
- [x] 不把 60×60/-130 升格为全局 UI 契约
- [x] 不授候选、不改 RELEASES

**不在范围内：**
- 再改注入实现
- 第五阶段锁定页

## Comments

- 2026-09-13 分类：map「spec 冻结正文不动，下次 fresh /to-spec 并入」——本票即该并入，不必重跑完整 /to-spec 会话。
- 2026-09-13 关单：五条期望行为逐段进 LIT 节（构造路径覆盖在册/既有 Tick 泵 16 拍节流首拍即试幂等补注入/非 Running 不新增/拆除失败引用保留→下次拆除重试/全页在册静默短路真做事才打日志），Testing §6 补泵路径五锚并点名 Plugin.Tests 组；地图 V4-R9 决策尾句改指 spec 正文（同行四锚含节名+票号+门禁）。红测缝=新 eng 门禁 `eng/Verify-SpecV4R9Ingested.ps1`（spec↔地图↔测试三向区域绑定），红 16 违例→绿，R2/R3 加块内/同行/节边界/禁用残留绑定并以 M1–M4 四突变各证红。src/tests 零改动、契约仍 2.1、不授候选；终态 Rebuild 0/0+7/7+fixture 门禁 PASS。双轴三轮（每轮全新实例）：R1 Spec gap（锚无上下文绑定）→R2，R2 余 gap（Testing 未绑节边界）→R3 全 CLEAN。审计 `audit/2026-09-13/POST-P4-06/review-loop.md`。
