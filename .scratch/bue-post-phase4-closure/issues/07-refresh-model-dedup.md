# 07: 管理面板 refreshModel 三处复制

Type: task
Status: resolved
Category: enhancement
Blocked by: 无（可立即开始）
Parent: [map.md](../map.md)

> *This was generated by AI during triage.*

## What to build

保存成功留页、确认离开后保存、刷新/重挂三条路径里，对「先重建目录条目再渲染」的复制收成一处。行为与 F2（保存后开关不回跳）保持一致。

## Agent Brief

**类别：** enhancement（重构）  
**摘要：** 去掉 refreshModel 三处复制，不改变保存后状态投影。

**当前行为：**
F2 在保存钮 / 确认留页 / 确认离开三条路径先重建条目再渲染，避免目录缓存的旧生命周期。Standards 标 deferrable：三处复制。产品正确。

**期望行为：**
- 单一「提交后刷新目录再画当前详情」入口，三条路径调用它。
- 回归：保存启停后开关与功能状态显示权威值，不回跳草稿前。
- 不改变确认三选一、草稿范围、跨源部分成功。

**关键接口：**
- 保存/确认导航后的目录重建
- DraftSaveReport 已提交的生命周期意图仍用于刷新后投影

**验收标准：**
- [x] F2 回归组仍绿（保存后状态不回跳）
- [x] 三路径不再各写一份刷新序列（审查能指出单一入口）
- [x] 全套测试绿；双轴 CLEAN
- [x] 不授候选

**不在范围内：**
- 插件草稿填充另两段同构（未升格）
- 改草稿语义

## Answer

单一入口=`BueNativeManagementPanel.RefreshCatalogThenPaintCurrentDetail`（`forceRefresh || CommittedLifecycleIntent` 才 `refreshModel()`，再按 Full/Details/None 画，banner 最后）。保存钮 / 确认留页 / 确认离开（脏刷新 `wasRefresh` 含）三路径改调它。Open / 干净刷新未抽（非 F2 门）。红测缝=新 eng 门禁 `Verify-RefreshModelDeduped.ps1`（红 7 违例→绿；M1–M4 各证红）。F2 行为组 `SaveCommittedLifecycleIntentFlag` 仍绿。契约仍 2.1、不授候选。双轴 R1 全新实例全 CLEAN。审计 `audit/2026-09-13/POST-P4-07/review-loop.md`。

## Comments

- 2026-09-13 分类：DEV-V4-09 结单 §6③。
- 2026-09-13 关单：F2 三路径 refresh-then-paint 收成 `RefreshCatalogThenPaintCurrentDetail`；红测缝=新 eng 门禁区域绑定+M1–M4 突变；tests/ 零改动（既有 F2 模型组继续钉行为）；src 只动原生面板适配器。双轴 R1 全 CLEAN 无递延。不授候选。
