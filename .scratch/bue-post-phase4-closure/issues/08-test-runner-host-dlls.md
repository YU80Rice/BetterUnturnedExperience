# 08: 干净克隆缺测试运行器宿主程序集（bin 依赖工作区历史拷贝）

Type: task
Status: ready-for-agent
Category: bug
Blocked by: 无（可立即开始；由 POST-P4-02 关单终验顺带抓出）
Parent: [map.md](../map.md)

> *This was generated by AI during triage.*

## What to build

`git clone` 后全解构建 exit 0，但**直接跑 7 个测试 exe 时 Plugin.Tests 在 `AssertSingleDllAssemblyClosure` 抛 `FileNotFoundException: BepInEx, Version=5.4.23.5`**——运行器需要的宿主程序集（BepInEx/0Harmony/Assembly-CSharp/UnityEngine.* 等）不随构建进 bin：引用全是 `<Private>False</Private>`，而主工作区 `tests/*/bin/Release/` 里能跑是因为存在历史上的 `../Libs/*.dll` 逐字节拷贝（不在 git、Rebuild 的 Clean 也不清非清单文件）。干净克隆/CI 环境没有这批拷贝就没有可运行的测试面。

## Agent Brief

**类别：** bug  
**摘要：** 让「克隆→构建→跑 7 exe」在干净机器上成立。

**当前行为：** 见上。POST-P4-02 终验实录：未配 DLL 的克隆首跑 6/7 绿、Plugin.Tests 红；从 `../Libs` 按主工作区 bin 名录制后 7/7 绿（`audit/2026-09-13/POST-P4-02/clone-plugin-firstattempt-fail.txt` + `clone-green-summary.txt`）。

**期望行为：**
- 构建后 bin/Release 自足（宿主 DLL 进输出目录或等价机制），克隆可直接跑全套。
- `*.dll` 全局忽略不变：靠构建时拷贝（如测试工程引用 `Private=True` 或 CopyFilesToOutputDirectory 项），**不**把 DLL 扫进 git。
- 注意副作用评估：Plugin.Tests 有 `AssertSingleDllAssemblyClosure` 与程序集闭包类断言，输出目录变满后语义是否要调（可能正是要它断言「闭包自足」）。

**关键接口：**
- 各测试 csproj 的 `<Reference>`/`<Private>` 与输出闭包
- `Program.AssertSingleDllAssemblyClosure`（版本 5.4.23.5 钉死？升级面）

**验收标准：**
- [ ] 干净克隆（worktree 法）构建后不做任何手工拷贝，7 exe 全绿
- [ ] `git status` 无新增 DLL 入库；`*.dll`/`*.log` 规则零放宽
- [ ] 全套 7/7 在工作区同样成立；门禁 eng/Verify-TestFixturesTracked.ps1 若适配 CopyToOutput 新写法则同步跑绿
- [ ] 双轴 CLEAN

**不在范围内：**
- CI 基础设施；Libs 内容版本升级（BepInEx 换版另票）

## Comments

- 2026-09-13 POST-P4-02 关单终验顺带抓出（票 02 范围=夹具入库，已闭环；运行器宿主 provisioning 独立成票）。
