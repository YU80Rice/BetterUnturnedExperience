# DEV-V2-25 结单报告 — LIT 整理网络：定向发送失败退避 + 告警限频 + 传输持续失败面可见化

- 日期：2026-09-09
- 票面：`.scratch/bue-v2-phase2-official-adoption/issues/DEV-V2-25-lit-tidy-send-backoff-ratelimit.md`
- 状态：**resolved（双轴 R2 双 CLEAN 闭环）**
- 优先级：P1（用户确认，票面 P2→P1）
- Origin：DEV-V2-24 采集期 F-C 具名延期（机上四轮量化 1199/1057/8333/1650 条逐帧 WARN）

## 1. 交付面（生产代码）

| 文件 | 内容 |
|---|---|
| `src/BetterUnturnedExperience.Lit/Tidy/LitTidySendHealth.cs`（新） | `LitChallengeRearmBook`：挑战重臂退避（代际键控，区间表 {0,1000,2000,4000,8000}ms——首重试立即=DEV-V2-24 F-A 冻结契约「下一拍重发现」，第二次起沿 BueNetworkRuntime 握手重探先例 1s→8s 封顶）；`LitSendFailureRateLimiter`：（generation, 发送结果种类）序列——首条 WARN+此后每累计 50 条一条（附累计=N）；同类连续失败达 10 上抛恰一条 `BUE-LIT-003 event=link-degraded`（generation/lastResult/consecutiveFailures 结构字段）；成功拍 `link-recovered` 并清零；同类切换/代际丢弃即重置 |
| `src/BetterUnturnedExperience.Lit/Tidy/LitTidyNetService.cs` | 构造注入 `Func<DateTime> clock`（默认 UtcNow，可测）；`OnSessionEstablished` 退避门（未到期代际保持 TRACKED 停泊，跳过 scope/book/challenge 工作）；`Tick` 新增 `DriveDueRearms` 重臂驱动（先快照后重入，防同拍重入/枚举变异）；`sessionEventsWired` 一次性接线守卫（消除重臂路径对同一会话重复订阅 Disconnected/GenerationChanged——DEV-V2-24 v6 主机日志「会话代际更替」双份行根因）；`OnSessionDropped`/`Stop` 清理两本 book 与接线表；`TrySendToSession` 接入限频+降级上抛 |
| `src/BetterUnturnedExperience.Plugin/BetterUnturnedExperience.Plugin.csproj` | 白名单 Include 1 行（LitTidySendHealth.cs，Link 与兄弟 Tidy 文件同构） |

有效性量化（harness 组断言，模拟 7 分钟持续失败窗）：尝试 8333→55、WARN 8333→2、结构性诊断恰 1 条；F-A「一帧抖动下一拍恢复」契约保留（既有红测未改且过）。

## 2. 红测先行（TDD 证据链）

1. **观测红**：`red-build.log`——CS0246 ×2（LitChallengeRearmBook/LitSendFailureRateLimiter 不存在）+ CS1729（LitTidyNetService 无 6 参构造）= 三缝编译红。
2. **实现**：上述三文件落地。
3. **观测绿**：旗标 `--bue-v2-lit-sendhealth-red` 收集式 ALL GREEN 3 组（`sendhealth-green-run2.log`）——重臂退避真值表 / 限频+降级+恢复清零 / harness 持续失败全链。
4. 票面 Acceptance 三断言逐条对应：WARN 条数有界（1000 条失败→20 条 WARN 断言；harness 55 尝试→2 条）、退避间隔单调（0,0,1000,2000,4000,8000,8000 断言）、恢复后清零（NoteSuccess 后新失败首条立即 WARN+降级阈值重开断言）。
5. 测试基建：`AssertBueV2LitSendHealth` 入默认套件+旗标双接线（F-E 教训：缺一即未完成）；`LitMultiplayerHarness.Create` 增可选 clock 参数（既有 6 处调用点零改动）；`SustainedFailureNetwork`/`FakeClock` 注入器。

## 3. Scope 4 —— 证据 B 轮 LHT/LIR 同期发送结果核对（研究项）

- **具名缺口**：证据 A/B 原始诊断包（210433/225112/225127，litfb7 轮）机器侧采集、未归档入库（全库+git 全历史+游戏目录+TEMP 均无）——「challenge 曾送达后中途劣化」读法无法从原始数据重放。
- **等效核对**（库内归档 v6/v7 P2P 主机+客机完整日志，全量行号见 `scope4-evidence-check.md`）：v7 gen=2 风暴（×1650）紧贴引擎级 `HOST_AUTHENTICATE_RECEIVED`（t=193.2s）终止，其后同会话 TidyCommitted reqId 1/2/3 与 LHT 广播 ×18 result=Sent 全部成功；v6 三个代际各自诞生窗 600/2808/3505 条风暴，challenge 全部最终送达应用。
- **结论（收窄票面「通道选择性」）**：失败呈**时间窗选择性**（传输就绪窗/劣化窗，就绪窗边界=引擎级 P2P 认证完成拍），非按消息种类或频道的选择性。证据 B 读法与该机制兼容，原始包缺口保留不影响修复面成立。

## 4. 双轴独立审查链（每轮两轴全新实例并行，零上下文，未合轴）

| 轮 | Standards 轴 | Spec 轴 | 处置 |
|---|---|---|---|
| R1 | **CLEAN**（1 NC + 4 SMELL 可延期） | **NOT CLEAN**（1 GAP + 1 DEVIATION） | F1 |
| R2 | **CLEAN**（0 发现；4 项 SMELL 同意具名延期） | **CLEAN**（0 发现；抽验证据文件行号引文属实） | 闭环 |

- R1 双轴共同发现：既有 DEV-V2-21 收集函数 `AssertBueV2LitMultiplayerPath` 抛错前缀被误写成 `DEV-V2-25 red collection`（复制残留）。F1=恢复 V2-21 前缀。
- R1 Spec GAP（Scope 4 无可复核交付物）：F1=新建 `scope4-evidence-check.md`（缺口登记+逐行号核对+复核命令）+票面 Comment 回填+源码头注释指向证据文件。
- 验证：F1 后全方案 Rebuild 0 错 0 警 + 旗标绿 + Plugin 默认套件 PASS（f1-*.log），R2 双轴全新实例确认闭环。

### 具名延期（不阻塞，逐条列出）

1. **CQS 混合**：`ShouldWarn`/`ShouldReportDegradation` 查询即记账——调用点顺序固定（先记数后判级），与仓库 book 风格一致。
2. **退避表字面量**：Core 的 `HelloReprobeInitialMs/CapMs` 为 internal 且无对 Plugin 的 IVT，不可引用；以注释钉先例来源。
3. **墙钟 DateTime.UtcNow**：与 Lit 域既有 TTL book（LitExpiringKeySet 等）一致；clock 已缝化，系统时跳变影响可接受。
4. **测试布局分散**：组方法与锚点分处 Program.cs 两处——既有单文件测试神文件约束内，随全域治理另案。

## 5. 全套验证（终态留档）

- 全方案 Rebuild：0 错 0 警（`f1-sln-rebuild.log`）
- 7 个测试 exe 直跑全 PASS（`final-fullsuite-{ClientUi,Contracts,Network,Placement,Plugin,Release,Settings}.log`）
- 旗标 `--bue-v2-lit-sendhealth-red`：ALL GREEN 3 组（`f1-sendhealth-run.log`）

## 6. 身份与发布节奏

- 本票为**测试侧+生产源修复**，按票面 Acceptance「候选重授随 DEV-V2-24 之后的发布节奏」：**本票不重授候选**。当前发布物 v7=A1B339BF…71359 先于本票源变更；下一次候选构建（DEV-V2-25 改动随入）时按 output-review-loop 授予新身份并更新 RELEASES 行。
- 本票生产 DLL 未单独授 CaseId（中间产物不授身份）。
