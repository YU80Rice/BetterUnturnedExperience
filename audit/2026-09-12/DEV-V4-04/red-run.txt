DEV-V4-04 红测证据（先红后绿，全部在本会话实测观察）

1) 运行时红 — ClientUi BII 退役（DevV4BiiRetireTests，未实现时）：
   DEV-05/.../DEV-V4-04 ClientUi tests: FAIL
   System.InvalidOperationException
   DEV-V4-04 BII retire: BII 快照只剩一行（Enabled 退役）

2) 运行时红 — Plugin facet 退役锚（--bue-v3-lifecycle-red，未实现时）：
   DEV-14 official registration parity tests: FAIL
   System.InvalidOperationException: DEV-V3-03 red collection (2):
     LIT facet 退役：目录条目不再声明 enabled 描述符（schema 退役→设置页无 enabled 行）
     LIT facet 退役：启动路径不再为它组装设置 runtime（旧总开关彻底退出 schema 与面板）

3) 编译红 — 新 Core 类型未存在（Settings.Tests / Plugin.Tests 同轮）：
   error CS0246: 未能找到类型或命名空间名"FeatureLifecycleIntentStore"
   error CS0246: 未能找到类型或命名空间名"LegacyEnabledAlias"
   error CS0103: 当前上下文中不存在名称"LegacyEnabledMigration"
   （先例：DEV-V2 网络缝红测以 CS0234 观测同型编译红）

4) 绿后全绿：Settings.Tests / ClientUi.Tests / Plugin.Tests PASS，
   其余四工程（Contracts/Network/Placement/Release）原锚全绿，见 run-*.txt。

测试自纠记录（红绿迭代中修正的是测试自身缺陷，非放宽判据）：
- Settings.Tests 引擎桩未模拟"退役动作自守契约"→ 修桩（键不在=无写）；
- LHT 在测试宿主上 Start 即 TypeInitializationException→Isolated（宿主局限，
  与迁移无关）→「不改生命周期」断言改为迁移前后状态/修订原样；
- WriteLegacyToggleDoc 对 true 值的提交是 no-op 不落盘 → 改两段提交；
- 阶段提交的 ExpectedRevision 以第一段返回的实际 revision 为基准；
- 失败注入组：EnsureCreated 抢占 Default → 改用 EnsureCreatedWith 显式注入；
- 失败注入组：机钩(Default)与引擎(StoreFor)实例分裂使故障未命中 →
  EnsureCreatedWith 同注册根键表=生产拓扑（一库两消费者）；
- 留痕断言行名修正：库自身发 result=commit-failed（机钩发 result=record-failed）。
