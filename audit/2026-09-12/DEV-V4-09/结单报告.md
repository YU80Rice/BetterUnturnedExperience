# DEV-V4-09 结单报告（Phase-4 可视化体验·画面验收与唯一对外候选）

Status: **RESOLVED·已关单**（2026-09-13 用户批准原话「批准，关单」→ 关单提交=RELEASES 行 12 当前发布物 + `publish/第4阶段-正式交付版本/` 四件套 + 本票 resolved；`39573c2` 修复轮 + `c8358e1` 实时注入轮 + `ffa84c6` 静默短路轮 + `d6f3fe2` F4 路由轮，双轴共八轮闭环）

## 0. 一句话

一轮实机抓三缺陷（v1 作废）→ F2/F3 修复实机确认；F1 两轮证伪后用户裁定实时注入（V4-R9）→ 用户再预警日志刷行属实→静默短路；**三轮 SP 实机 F1/⑦/⑧/④ 全双证据 PASS**；⑨ 暴露 **F4 同键路由缺陷**（插件 GUID=功能 id 时功能页永被同键插件行遮蔽）→ `d6f3fe2` (种类,StableId) 双键修复（双轴 CLEAN）→ 候选 v5 `e20fea58…` 部署双端 + U3DS 负面重验成立；停等 ⑨ 复测 + P2P。

## 1. 候选身份（开发态，批准后方定身）

| 项 | 值 |
|---|---|
| **当前候选 SHA-256（v5）** | `E20FEA58FFD888FC127CB7ECBAA47B12ECE8370957E84CDA5E16BB70B605AEEE` |
| 字节 | 636416 |
| 确定性 | 3× `-t:Rebuild` 逐字节一致（`candidate-sha256-final.txt`），全 exit 0、0 警 0 错 |
| **作废前序** | `E3D199CD…40C9`（v4，F4 同键路由缺陷）→ `4EC05965…8EFA0`（v3，稳态日志刷行）→ `E8687DE5…551F86`（v2，Start 尾部证伪）→ `C57C666C…8D6C2E`（v1，三缺陷本体）。不得用于继续采集 |
| CaseId | `DEV-V4-09-CANDIDATE-20260912` |
| 候选纪律 | 01..08 未授候选；RELEASES 无 DEV-V4 行；契约仍 2.1（F4 修复全 internal，`ContractTypes.cs` 零 diff） |
| 源基线 | `d6f3fe2`（链 `aaf939d`→`39573c2`→`c8358e1`→`ffa84c6`→`d6f3fe2`） |
| 部署 | SP + U3DS 双端 certutil 复核 = `E20FEA58…` |
| NoOp 探针 | `BetterUnturnedExperience.NoOpFixture.dll` `a2740d80…bc0f5` 已部署 SP 端（案例⑨用，非候选本体，F4 修复未触及夹具） |

## 2. 实机缺陷与修复史（红测先行 + 双轴审查）

- **F2（保存后状态回跳）**：`DraftSaveReport.CommittedLifecycleIntent` + 三路径先 `refreshModel()` 再渲染。**二轮实机确认生效**。红测=ClientUi `SaveCommittedLifecycleIntentFlag`。
- **F3（外部文本无法修改）**：`OnTextChanged` 逐键入草稿+Enter 提交渲染+Esc 回拨。**二轮实机确认生效**。具名 seam gap（工单 Comments）：原生接线层无宿主红测锚，实机为最终验证面。
- **F1（按钮永不复装）三轮**：v2 Start 尾部补注入被二轮实机证伪（Start 运行在机器 Starting 态，九态门必然自我拒绝）→ **v3 实时注入**（用户裁定 V4-R9：Q55 九态门不变，Running 态经既有 Tick 泵 16 拍节流对存活仪表盘幂等补注入；泵在册去重/ctor 覆盖旧行为并存）→ **v4 稳态静默短路**（用户预警日志刷行属实→第四道门全页在册静默 return，部分在册仍重试自愈）。红测=泵组全边界（满册后 20 拍零 [TidyUI] 行）。**三轮实机确认生效**（3 个停→启周期拆除/复装全轨迹 + 用户目视 + 全程 [TidyUI] 仅 42 行/45min）。
- **F1b（诊断失明）**：Stop 解绑 LogSink 后 Start 重绑（Lit/Lir/Lht 同构）。三轮 [Tidy] 全轨迹在场=生效实证。
- **F4（NoOp 功能页不可达，三轮发现）**：根因=生态标准形态下插件 GUID 与功能 id 同串（NoOp 样板 `io.github.yu80rice.bue.noop`），侧栏两行共享 StableId，A-Z 排序插件显示名行在前，`RenderDetails` 按 StableId 单键取首行→两行都渲染插件分支（夹具无 cfg=空页），功能页（探针 Toggle+Choice）永不可达。修复=`d6f3fe2`：模型 `OpenDetail/TryLeaveDetail` 加 kind 重载（kind 给定时只解析该种类、换种类=重置草稿脏不跨页、显式种类无条目如实无草稿不回落；单参=features-first 旧语义兼容），原生面板 `selectedKind/pendingNavigationKind` 随点击行携带（确认导航/刷新重挂/关闭/回退首行全路径）。红测=编译红 CS1501×6→ClientUi `CollisionFeatureAndPluginShareStableIdRoutesByKind`（种类分别受理/拒绝、分别保存单源、换种类清脏、确认导航带种类、单参兼容锚）。具名 seam gap（F3 先例同口径）：Glazier 路由层宿主无测面，⑨实机复测=最终验证面。具名 deferrable：收藏星与重启徽章仍 StableId 单键（同键族残留，NoOp 样板唯一触发，另票）。
- 双轴链：Standards R1 blocking(具名 gap)→R2 CLEAN；Spec R1/R2 CLEAN；Standards R3 CLEAN；Spec R3 范围蔓延→V4-R9 裁决闭合→R4 CLEAN；Standards R4 + Spec R5（静默短路）双 CLEAN；**F4 轮 Standards R6 CLEAN（3 deferrable：插件草稿填充两段同构/收藏徽章单键属实/「显式种类无条目不回落」未钉红测——均不升格）+ Spec R6 复判 CLEAN**（首轮 blocking=把票级关单门前置为代码轮审查门，澄清流程时序后撤销；Q22/Q29 边界自洽、V4-R9 零触碰、契约 2.1 纯 internal 均获确认）。
- 构建与全套：每轮 `-t:Rebuild` 0 警 0 错 + 全套 7/7 PASS。

## 3. U3DS headless 负面不变量 · v5 重验 **PASS**（2026-09-13，绑定 `E20FEA58…`）

`auto-evidence/u3ds/u3ds-headless-cleanboot-e20fea58-20260913.log`（102 行）+ `u3ds-tally-v5-e20fea58.txt`：identity×1 绑 v5、5×accepted=True、`runtime-gate decision=Headless`+LIT to=Running（门禁拒武装）、posted=0、BUE Error=0/unhandled=0、`[TidyUI]`=0、心跳×2（泵活）、无 `.bue-settings` 落盘。v1..v4 会话证据同目录留档。

## 4. SP/P2P 画面案例进度

- **一轮 PASS（v1，仍有效）**：①初始态双图 ②脏态 ③保存成功（持久化三重验证）⑤确认框。
- **二轮 PASS（v2）**：⑥前半（停用→按钮拆除）+F2/F3 修复确认；⑥后半/⑦⑧被 F1 阻断。
- **三轮 PASS（v4，包 001728 归档 `auto-evidence/sp/round3-20260913/`+tally）**：**F1 复验**（3 个完整停→启→复装周期+用户目视）；**静默短路**（42 行/45min 全为一次性突发）；**⑦**（本地整理已提交×4）；**⑧**（SPF cfg 哈希 0a73933d→579841a8 磁盘实锤；UPM 本机未装=面板不画遗留 cfg 为正确行为，④改用 SPF）；**④**（agent `attrib +R` 锁 SPF cfg→逐字「未保存：配置文件无法写入。」+草稿保留截图→解锁哈希不变=零落盘）；47 条 [Error 逐条甄别全良性（基线同型/设计内负探针/SPF 域另案）。
- **⑨ PASS（v5 四轮，包 114109 归档 `auto-evidence/sp/round4-20260913/`+tally）**：用户目视「功能页和负例均可实现」=功能行详情页探针开关+档位（甲/乙）可见可操作保存、插件行空页正确负例；磁盘实锤 noop ClientPreference mtime=11:40:48/revision=10/两键 base64 对应；**红利**=用户在生态功能页完成停用→再启用（UserDisabled gen7→gen8 干净拆重装=05 表面在第三方功能成立）。BUE [Error=2=设计内负探针×2 代际，零崩溃。
- **P2P PASS（v5 五轮，主机包 120321/客机包 120223，归档 `auto-evidence/p2p/round5-p2p-tally.txt`）**：双端 identity 绑 v5；跨端整理实锤=主机 `[TidyNet] -> 客机 TidyCommitted(reqId=1/2)` 代执行×2+双端 runtime-arm+用户目视客机整理可用。用户报「主机网络模块 Isolated+启用失败」经取证=**非新缺陷非 v5 回归**：module-start not-started→Isolated 与 v7/v8/2.1 基线逐字同型（V3-09 §4c 良性投影）；5 次启用=5 次真实重启尝试 `enable-failed stage=start` 干净回 Isolated+逐字「未保存：功能启停失败。」+草稿保留=Q44 诚实失败面按设计工作；「隔离但功能在线」文案误导=V3-09 §6 具名后续另票。跨端判据以更强形式成立（模块最重态整理仍照常；显式停用腿由 SP LIT/NoOp 覆盖）。

## 5. 关单流程（批准后同一次提交）

⑨ 复测 + P2P PASS → 人工批准「发布」→ 同提交：`RELEASES.md` 行 12 + `publish/第4阶段-正式交付版本/` 换新 + 本票 resolved + 工单/审计入库（v1..v4 作废留档）。

## 6. 关单记录

**已关单（2026-09-13）**：用户原话「批准，关单」。同一次提交：`RELEASES.md` 行 12（当前发布物=v5 `E20FEA58…AEEE`，行 11 改已取代）+ `publish/第4阶段-正式交付版本/`（DLL+玩家手册+SDK 契约文档[2.1 附录版逐字同]+交付说明）+ 本票 resolved + 全套证据入库（v1..v4 作废留档；SP round3/round4、P2P round5、U3DS v5 tally）。

具名后续（另票，不阻塞关单）：①「网络模块隔离但功能实际在线」投影文案误导澄清/改判据（V3-09 §6 起具名，P2P 五轮再实证）；②收藏星与「需要重启」徽章 StableId 单键残留（同键族，NoOp 样板唯一触发）；③模型层 refreshModel 三处复制重构（Standards deferrable）；④「显式种类无条目不回落」红测补钉（Standards R6 deferrable）。
