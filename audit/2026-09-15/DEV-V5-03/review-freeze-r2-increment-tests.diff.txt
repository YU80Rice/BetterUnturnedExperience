--- r1/new-test-file
+++ r2/new-test-file
@@ -160,6 +160,18 @@
                 "原因文案：成功无原因文案（None 为空串）");
             check(LitContainerTidyReasons.TryFromWire(0, out _) && LitContainerTidyReasons.TryFromWire(8, out _) && !LitContainerTidyReasons.TryFromWire(9, out _),
                 "原因编码：线协议字节域冻结 0..8，越界 fail-closed");
+
+            // 1d. 容器标题栏 surface adapter：布局/文案出口在容器侧，不复用服装页右侧锚偏移。
+            check(LitContainerTitleBarAdapter.HeaderIndex == 5,
+                "标题栏 adapter：容器页 = headers[STORAGE - SLOTS]（第 6 颗标题，与服装页五颗分离）");
+            check(LitContainerTitleBarAdapter.PositionScaleX == 0f && LitContainerTitleBarAdapter.PositionOffsetX >= 0f,
+                "标题栏 adapter：容器按钮左锚定位（服装页 -130 为右侧锚，不复用）");
+            check(LitContainerTitleBarAdapter.ButtonSizeX > 0f && LitContainerTitleBarAdapter.ButtonSizeY > 0f,
+                "标题栏 adapter：尺寸由 adapter 提供");
+            check(LitContainerTitleBarAdapter.ButtonText == "整理",
+                "标题栏 adapter：文案仍叫「整理」（不叫整理箱子/后备箱）");
+            check(!string.IsNullOrWhiteSpace(LitContainerTitleBarAdapter.TooltipText),
+                "标题栏 adapter：提示单源在容器侧");
         }
 
         // ─────────────────────────────────────────────────────────────────
@@ -231,11 +243,14 @@
             LitContainerSessionKind kind = LitContainerSessionKind.WorldContainer,
             bool active = true, bool authorized = true, ulong fingerprint = 1000UL)
         {
+            // authorized 映射到声明种类自己的授权维度（两个 adapter 各读各的）。
             return new LitContainerLiveFacts
             {
                 KindObserved = kind,
                 SessionActive = active,
-                RequesterAuthorized = authorized,
+                WorldOpenerIsRequester = authorized || kind != LitContainerSessionKind.WorldContainer,
+                WorldAccessAllowed = authorized || kind != LitContainerSessionKind.WorldContainer,
+                TrunkDriverAuthorized = authorized || kind != LitContainerSessionKind.VehicleTrunk,
                 LiveFingerprint = fingerprint,
             };
         }
@@ -281,6 +296,33 @@
                 "重验顺序：无权限优先于版本（先拒身份再比内容）");
             check(LitContainerTidyVerifier.Verify(Live(kind: LitContainerSessionKind.VirtualContainer, fingerprint: 9), Claim()) == LitContainerTidyReason.UnsupportedKind,
                 "重验顺序：类型不支持先于版本比对");
+
+            // 3b. 两个 adapter 按各自种类处理权限维度（T4 Q1：箱子=单 opener/锁组；后备箱=驾驶座）。
+            check(LitContainerTidyVerifier.Verify(new LitContainerLiveFacts
+                    { KindObserved = LitContainerSessionKind.WorldContainer, SessionActive = true,
+                      WorldOpenerIsRequester = false, WorldAccessAllowed = true, TrunkDriverAuthorized = true,
+                      LiveFingerprint = 1000UL }, Claim()) == LitContainerTidyReason.LostAccess,
+                "按种分权：世界箱 opener 已换 = 已失去权限（单 opener 维度由世界 adapter 判定）");
+            check(LitContainerTidyVerifier.Verify(new LitContainerLiveFacts
+                    { KindObserved = LitContainerSessionKind.WorldContainer, SessionActive = true,
+                      WorldOpenerIsRequester = true, WorldAccessAllowed = false, TrunkDriverAuthorized = true,
+                      LiveFingerprint = 1000UL }, Claim()) == LitContainerTidyReason.LostAccess,
+                "按种分权：世界箱原版锁/组事实拒绝（checkRot）= 已失去权限，BUE 不另造锁");
+            check(LitContainerTidyVerifier.Verify(new LitContainerLiveFacts
+                    { KindObserved = LitContainerSessionKind.VehicleTrunk, SessionActive = true,
+                      WorldOpenerIsRequester = true, WorldAccessAllowed = true, TrunkDriverAuthorized = false,
+                      LiveFingerprint = 1000UL }, Claim(kind: LitContainerTidyKind.VehicleTrunk)) == LitContainerTidyReason.LostAccess,
+                "按种分权：驾驶座不再有效（离座/换座/换车）= 已失去权限（由后备箱 adapter 判定）");
+            check(LitContainerTidyVerifier.Verify(new LitContainerLiveFacts
+                    { KindObserved = LitContainerSessionKind.VehicleTrunk, SessionActive = true,
+                      WorldOpenerIsRequester = false, WorldAccessAllowed = false, TrunkDriverAuthorized = true,
+                      LiveFingerprint = 1000UL }, Claim(kind: LitContainerTidyKind.VehicleTrunk)) == LitContainerTidyReason.None,
+                "按种分权：后备箱判定不读世界箱维度（世界箱维度全假仍放行）");
+            check(LitContainerTidyVerifier.Verify(new LitContainerLiveFacts
+                    { KindObserved = LitContainerSessionKind.WorldContainer, SessionActive = true,
+                      WorldOpenerIsRequester = true, WorldAccessAllowed = true, TrunkDriverAuthorized = false,
+                      LiveFingerprint = 1000UL }, Claim()) == LitContainerTidyReason.None,
+                "按种分权：世界箱判定不读后备箱维度（驾驶座维度全假仍放行）");
         }
 
         // ─────────────────────────────────────────────────────────────────