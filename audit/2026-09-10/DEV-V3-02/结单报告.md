# DEV-V3-02 结单报告 — FeatureEventBus 事件归属路由（三元组索引+类型归属登记 seam）

- 日期：2026-09-10
- 票面：`.scratch/bue-v2-phase3-platform/issues/DEV-V3-02-feature-event-bus.md`
- 状态：**resolved（双轴 R2 双 CLEAN 闭环）**
- 候选纪律：本票为 Phase-3 中间票——**不产正式候选 DLL、不更新 RELEASES、不授 CaseId**（spec 候选策略：01..08 中间构建=开发态内部基线；唯一 2.1 候选随 DEV-V3-09 授予）

## 1. 交付面（生产代码）

| 文件 | 内容 |
|---|---|
| `src/BetterUnturnedExperience.Contracts/ContractTypes.cs` | Minor 2.1 加性新面：`FeatureEventRegistrationReason`（`None=0/InvalidEventId=1/EventIdNotDerivedFromOwner=2/EventTypeAlreadyRegistered=3/EventIdAlreadyRegistered=4`，byte 枚举）、`FeatureEventRegistrationResult`（readonly struct：Registered/Reason/DiagnosticId，对齐 `FeatureRegistrationResult` 先例）、`IFeatureEventRegistry.Register<TEvent>(string eventId)`、`IFeatureBootstrap.EventRegistry`（第 11 成员，可用性矩阵行=DEV-V3-02 起可用）。两既有事件接口（IFeatureEventSubscriber/IOwnedFeatureEventPublisher）形状与语义零改动 |
| `src/BetterUnturnedExperience.Core/Events/FeatureEventBus.cs` | ①路由索引=（EventId, EventType, owner）三元组：`ownedTypes`（Type→记录）+`ownedEventIds`（Ordinal）双字典，一类型恰一归属 EventId、一身份串恰一类型；②构造期官方归属种子（宿主组合期唯一登记）：TidyCompleted→`io.github.yu80rice.bue.inventory-tidy`、HostTick→`io.github.yu80rice.bue.host`，owner=身份串前缀（与登记同一条 `<owner>/<event-name>` 派生规则），不可重登记/不可改挂；③公开登记入口 `EventRegistry(FeatureId)`：宿主保留身份 fail-fast（与 Publisher 视图同门）；④登记管线：格式(001)→前缀(002)→类型重复(003)→身份串冲突(004)，全部显式结果+结构化诊断不抛越界异常，重复登记拒绝不覆盖，sink 回调全在锁外（R1 Standards 发现锁内回调后修复）；⑤发布路由（T3 冻结流程）：前缀保留→类型已登记(reason=type-not-registered)→owner==归属(reason=owner-mismatch)→eventId==归属身份串(reason=event-id-mismatch)→按类型快照派发；任一拒绝=显式 false+诊断+零派发；⑥TryPublishHost 同样过归属索引（owner 须为宿主保留身份）；⑦订阅门：未登记类型=诊断+`ArgumentException` fail-fast（同 null handler 纪律）。既有语义保持：锁内快照锁外派发、单 handler 异常隔离进诊断、句柄幂等、UnsubscribeAll 停止边界 |
| `src/BetterUnturnedExperience.Core/Registration/FeatureBootstrap.cs` | 构造器加第 6 参 `IFeatureEventRegistry eventRegistry` + `EventRegistry` 属性（组合记录，无自造面） |
| `src/BetterUnturnedExperience.Plugin/BueFeatureStartRuntime.cs` | `StartCatalog` 组装 `bus.EventRegistry(feature)`（矩阵行：EventRegistry 自本票起永非 null）；注释更新矩阵口径 |

## 2. 红测先行（TDD 证据链）

1. **观测红·行为**（`red-plugin-routing-run.log`，6 条）：归属拒绝三断言（他人载荷类型经自有前缀身份串发布当时被放行——前缀规则抓不住的伪造）+未登记类型三断言（订阅未登记类型当时成功、发布当时放行、无诊断）。
2. **观测红·编译**（`red-contracts-compile.log` + `red-plugin-compile.log`）：CS0103/CS0246（`FeatureEventRegistrationReason`/`FeatureEventRegistrationResult` 不存在）+CS1061（`FeatureEventBus.EventRegistry`/`IFeatureBootstrap.EventRegistry` 未定义）。
3. **实现**：上表四文件落地。
4. **观测绿**（`green-plugin-routing-run.log`）：新组 9 子组全绿。中途修复一轮红→绿：诊断行断言暴露拒绝判定序与 T3 冻结流程不一致（event-id-mismatch 先于 owner-mismatch），按 T3 流程 ②归属→③owner 重排后转绿；官方消费锚伪造断言改走自有前缀身份串。
5. **F1 修复后回归**：R1 双轴 FINDINGS（锁内 sink 回调+冻结面清单未登记）修复后全套复绿（`f1-*-run.log`）。
6. 票面验收断言对应（Plugin.Tests `AssertBueV3EventOwnershipRouting` 9 子组+Contracts.Tests DEV-V3-02 锚块）：
   - 归属拒绝：thirdParty 以自有前缀身份串发布 TidyCompleted→false+owner-mismatch 诊断+零派发；
   - 前缀拒绝：既有 identity-not-derived-from-owner 路径回归绿（DEV-V2-19 组）+登记前缀 002 正反例（含宿主保留前缀）；
   - 登记成功：显式三元组（None/BUE-EVT-ACCEPT）→订阅+发布+载荷透传；
   - 登记失败：null/空/无分隔符/空名/空前缀→001，外域前缀/宿主前缀→002，均显式结果不抛异常+逐次诊断；
   - 重复登记：同类型同串→003、同类型异串→003（原映射不被覆盖：原串照常路由、第二串被拒）、同串异类型→004；
   - 未登记发布+订阅均拒：发布 false+type-not-registered 诊断+零派发；订阅 ArgumentException fail-fast+诊断；
   - 同类型唯一 EventId 不互收：登记后第二身份串无路由、仅登记串派发一份；
   - 宿主保留身份不可 mint：`EventRegistry(host)` fail-fast+功能登记宿主串 002+功能发布 HostTick 拒+HostTick 内部路径照常（DEV-V2-19 组回归）；
   - bootstrap 登记缝：真实 `StartCatalog` 组装 `EventRegistry` 永 non-null+探针经 bootstrap 视图登记成功；
   - 官方先行消费锚：**真实 LIT 模块**（`InventoryTidyModule.Start` 经宿主 bootstrap）经登记路径发布 TidyCompleted→真实消费方收到且载荷透传；消费方 owner 冒发→owner-mismatch 拒+零派发；
   - Contracts.Tests：Reason 枚举值冻结（0..4）+Result 三元组形状+`IFeatureEventRegistry` 接口+`IFeatureBootstrap.EventRegistry` 成员存在。

## 3. 双轴独立审查链（每轮两轴全新实例并行，零上下文，未合轴）

| 轮 | Standards 轴 | Spec 轴 | 处置 |
|---|---|---|---|
| R1 | **FINDINGS**（1 硬伤：`Register` 003/004 拒绝路径在锁内回调 diagnosticSink，违反锁内不做回调纪律；2 deferrable：`OwnedEventType.EventType` 死字段、`RegisterOfficial` 静默 return） | **FINDINGS**（1 gap：票面「登记 seam 登记入冻结面变更清单」未落登记动作） | F1 |
| R2 | **CLEAN**（2 deferrable：`int duplicate` 三态 primitive obsession 判断题、RegisterOfficial 静默 return=票面具名延期） | **CLEAN**（0 发现；EventId 匹配位于 owner 门之后核实与 T3 冻结流程相容） | 闭环 |

- F1 修复：①003/004 拒绝判定收进锁内、诊断与返回移到锁外（`duplicate` 三态握手）；②死字段 `EventType` 清理（索引键即 ownedTypes 的 Type）；③冻结面变更清单登记入票面 Comments（七条目，SDK 条目归 DEV-V3-08 附录 A/B 总装）。
- Spec R1 判断口径核实成立（不构成 finding）：订阅未登记类型=诊断+fail-fast 异常（同 null handler 纪律，规格原文）；`EventRegistry(host)` fail-fast（「宿主保留身份不可 mint」票面原文）；EventId 匹配门序（T3 流程 ⑤前才进派发，eventId 匹配为路由完整性门）。

### 具名延期/判断口径（不阻塞，逐条列出）

1. **SDK 逐码文档**（BUE-EVT-ACCEPT/001..004 逐码处理建议+归属路由语义登记）→ DEV-V3-08 附录 A/B 总装，本票不做（候选纪律禁对外文档）。
2. **`RegisterOfficial` 构造期种子对不可达输入静默 return**：仅常量路径（TidyCompleted.EventId/HostTick.EventId 文法恒合法），生产不可达；加诊断属死代码噪音，具名保留。
3. **`int duplicate` 三态（0/3/4）**：锁外 sink 纪律的握手变量，局部 4 行控制流闭合；R2 判断为 primitive obsession 判断题，deferrable。
4. **可用性矩阵扩至第 11 成员**：`IFeatureBootstrap.EventRegistry` 为票面授权的 seam 形状决定权（「形状由本票定」）内的加性成员；矩阵行=DEV-V3-02 起可用，已入票面冻结面清单，DEV-V3-08 附录 A 按此落档。

## 4. 全套验证（终态留档）

- 全方案 Rebuild：0 错 0 警（`final-sln-rebuild.log`）
- 7 个测试 exe 直跑全 PASS（`final-fullsuite-*.txt`：Contracts/Network/Placement/Settings/ClientUi/Release/Plugin）
- 增量：`final-increment.diff`（6 文件，+528/−14 基线+F1 终态）
- 红绿证据：`red-plugin-routing-run.log`/`red-contracts-compile.log`/`red-plugin-compile.log`/`green-plugin-routing-run.log`/`green-contracts-run.log`/`green-plugin-fullsuite-run.log`/`f1-*.log`/`green-*-build.log`（`.gitignore` 纪律：log/diff 磁盘归档不入库，报告+final-fullsuite txt 入库）
