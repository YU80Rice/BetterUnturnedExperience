# DEV-V3-04 结单报告 — BueNetwork 传输规则与主线程投递（发送预算+MainThread dispatcher+链路健康+回放投影）

- 日期：2026-09-10
- 票面：`.scratch/bue-v2-phase3-platform/issues/DEV-V3-04-bue-network.md`
- 状态：**resolved（双轴 R2 双 CLEAN 闭环）**
- 候选纪律：本票为 Phase-3 中间票——**不产正式候选 DLL、不更新 RELEASES、不授 CaseId**（spec 候选策略：01..08 中间构建=开发态内部基线；唯一 2.1 候选随 DEV-V3-09 授予）

## 1. 交付面（生产代码，17 文件 +1396/−261）

| 文件 | 内容 |
|---|---|
| `src/BetterUnturnedExperience.Contracts/ContractTypes.cs` | ①`NetworkSendResult.Throttled=205`（ushort 加性值，不复用；旧模块安全降级纪律写入枚举注释）；②`IFeatureBootstrap` 第 12 成员 `IFeatureMainThread MainThread`（类型名/成员名本票定；矩阵行=DEV-V3-04 后可用+未接线宿主 null 阶段基线容忍）；③`IFeatureMainThread`（恰一 `MainThreadPostResult Post(Action)`，注释冻结最小行为面五条+执行异常隔离+禁自建泵）；④`MainThreadPostReason {None=0,CapacityExceeded=1,GenerationInvalid=2}`+`MainThreadPostResult` 显式三元组（FeatureEventRegistrationResult 先例） |
| `src/BetterUnturnedExperience.Core/Dispatch/MainThreadDispatcherRuntime.cs`（新） | 平台主线程 dispatcher 本体（宿主组合，public≠SDK 契约）：全局容量 256/每拍 32（本票定值，可观察可测试）；FIFO；任务绑 (owner, LifecycleGeneration)——`OpenGeneration` 换代即撤旧代未执行任务（LIT EnsureOpen 重开先例注释）；`InvalidateOwner`（停/隔离撤账拒投）；`ShutdownHost`（宿主停止全拒）；投递管线=null fail-fast（BUE-MT-004 先行后抛）→失效（BUE-MT-002）→容量（BUE-MT-001）→受理（BUE-MT-ACCEPT），全显式结果+锁外发射；执行期单任务异常隔离 BUE-MT-003；泵线程守卫（构造线程外 Pump 拒绝 BUE-MT-005，主线程语义构造性保证） |
| `src/BetterUnturnedExperience.Core/Network/NetworkSendGuard.cs`（新） | 每会话（ConnectionGeneration）发送账本：固定窗 2000ms/256 条数据发送（本票定值）+连续失败健康序列（阈值 10=DEV-V2-25 先例；degraded 每 episode 恰一次/recovered 恰一次+清零）；Throttled 不计入失败（平台节流≠传输失败）；无自有锁（运行时 sync 下调用，时钟锁外采样传入，注释如实） |
| `src/BetterUnturnedExperience.Core/Network/BueNetworkRuntime.cs` | ctor 加性尾参 `Action<string> diagnosticSink = null`；三发送方法接预算/健康（判定锁内、帧执行与发射锁外；SendToServer 记账于已建立快照会话）；`SendTargets` 聚合扩展（纯节流→Throttled；混合含节流→PartialFailure 不静默报全 Sent；既有冻结组合原样保留）；入站 handler 异常从空吞改 BUE-NET-004 结构化诊断（channel/direction/generation/errorType，无 payload 字节，不扩散不打穿泵）；`RemoveSessionLocked`/`SetModuleActive(false)` 接 guard 代际清理（不留残留）；BUE-NET-001/002/003 行格式 |
| `src/BetterUnturnedExperience.Core/Network/DeferredBueNetworkApi.cs` | ctor 加性可选 sink；**空 catch 无痕折叠全部消除**（每 catch 诊断或注释）；回放失败四态投影 BUE-NET-005=not-ready（从未接线）/detached（撤线=模块停止家族）/replay-failed（stage=replay-register/replay-subscribe/replay-dispose）/transport-unavailable（live 调用异常，stage=send-to-server/-clients/-client、register、unregister、sessions、detach-dispose）；not-ready/detached episode 锁存（每 episode 恰一条防刷屏）；既有 DEV-V2-21 语义零改动 |
| `src/BetterUnturnedExperience.Core/Registration/FeatureBootstrap.cs` | 加性可选尾参 `mainThread`+getter（既有 10 参调用点原样编译=阶段基线 null 侧） |
| `src/BetterUnturnedExperience.Plugin/BueMainThreadRuntime.cs`（新） | 组合根（BueHostEventRuntime 先例）：懒建+runtime log sink；`TickOnce()` 组合级异常带（BUE-MT-006，R1-Standards deferrable 补码名）；`Shutdown(reason)` 宿主停止边界；`Clear()` 测试缝 |
| `src/BetterUnturnedExperience.Plugin/BueFeatureStartRuntime.cs` | ComposeBootstrap 组 MainThread 视图（`OpenGeneration`+`CreateView`，矩阵行兑现）；Isolate（start-fault/start-result/enable-fault/enable-not-started）与 StopEntry（停止/UserDisabled）边界=`InvalidateOwner`；StopAll(PluginStopping)=`ShutdownHost`；矩阵注释更新（MainThread 自本票非 null） |
| `src/BetterUnturnedExperience.Plugin/BueRuntimeCompletionChain.cs` | `BueRuntimeTickChain.Tick()` 在 HostTick 之后接 `BueMainThreadRuntime.TickOnce()`（平台唯一泵；链序注释更新） |
| `src/BetterUnturnedExperience.Plugin/NetworkModuleAdapter.cs` | armed runtime 与 Deferred 门面双双绑 `[BUE-NET]` 诊断 sink（宿主既有 DiagnosticLogSink 缝，T5 不新增公开 Logger 成员） |
| `src/BetterUnturnedExperience.Lit/Tidy/LitTidySendHealth.cs` + `LitTidyNetService.cs` | **`LitSendFailureRateLimiter` 退役**（类删除+退役注释：8333 条逐帧 WARN 形态从源头根除；`LitChallengeRearmBook` 业务重臂原样保留=裁决一「不上收」）；TrySendToSession 的逐帧 WARN/BUE-LIT-003 上下抛删除（结果如实返回供重臂决策，不静默）；DropGeneration/DropAll 接线撤除 |
| `src/BetterUnturnedExperience.Lir/InPlaceReloadModule.cs` + `Net/LirRepackNetwork.cs` | LIR 迁移=官方先行消费者：Start 捕获 `bootstrap.MainThread`→`NetService.BindMainThread`；`Drain()`=每拍向平台 seam `Post(DrainOnce)`（fire-and-forget；拒投可见+下一拍重投=业务对平台信号的处理节奏）；`MainThread==null`（未接线宿主/既有夹具）回退直跑=旧语义；业务队列（合并/优先/TTL/摘要）=功能私有保留 |
| `src/BetterUnturnedExperience.NoOpFixture/NoOpFeaturePlugin.cs` | 生态对照最小延伸：ProbeState +`MainThreadAvailable`/`MainThreadPosted`（Start 观察接线缝+投递受理；全链 probe 归 08） |
| csproj×2 | Core 白名单+Plugin 嵌入清单（Link 形）加 3 新文件 |
| tests×2 | Plugin.Tests 新组 `AssertBueV3NetworkTransportRules` 12 子组+`--bue-v3-network-red` 锚+NetworkV3Fixture（适配器泵线假引擎）+抛错假运行时；DEV-V2-25 组按退役更新（限频真值表组随类删除，harness 组改缺席断言）；01 矩阵组 MainThread 行更新；Contracts.Tests DEV-V3-04 锚块（Throttled=205 唯一+API 七成员未扩+IFeatureMainThread 单方法+Reason 值冻结+Result 三元组+矩阵成员） |

## 2. 红测先行（TDD 证据链）

1. **观测红·行为**（Round-1，`red-plugin-network-run.log` 24 条 + `red-lit-sendhealth-run.log` 6 条）：全部只依赖既有可编译 API（NetworkModuleAdapter 假引擎+DiagnosticLogSink 观察缝）——预算首拒/连续超限/恢复/跨代际清零/聚合/同权（10 条）、链路健康电平 degraded/recovered/清零重开/计数钉死（7 条）、入站异常诊断行（3 条）、回放 not-ready/replay-failed/detached 投影（4 条）、LIT 告警限频退役缺席锚（6 条：逐帧 WARN/BUE-LIT-003 上下抛归零）。红阶段修过两处测试自身缺陷（deferred 夹具先武装再喂 Hello 的时序；未记录为产品红）。
2. **观测红·编译**（Round-2，`red-plugin-compile.log` CS0103×20/CS0246×8/CS0234×8/CS1061×16/CS0117×2/CS1729×2；`red-contracts-compile.log` CS0103×14/CS0246×12/CS0117×2）：新契约面全引用（Throttled/IFeatureMainThread/MainThread 成员/11 参构造器/MainThreadDispatcherRuntime/BueMainThreadRuntime/NoOp ProbeState 新字段）。
3. **观测绿**（`green-plugin-network-run.log` 12 子组 ALL GREEN、`green-lit-sendhealth-run.log` 2 子组 ALL GREEN、`final-fullsuite-*.txt`×7 全 PASS）。
4. 验收条件对照：LocalLoopbackTransport/假传输驱动各先红后绿 ✅（第四态 transport-unavailable=R1-Spec gap→F1 补锚，实现分支已在→锚直接绿，缺口发现本身为红证据，R2 复核闭环）；矩阵接线两侧红测（MainThread 接线前 null=手工 10 参组装侧+接线后可用=真 StartCatalog；未知结果安全降级=LegacyModuleTreatsAsSuccess 形状+205 唯一性）✅；官方先行消费锚（LIR 真模块真 dispatcher：入站只入队→OnHostTick 投递内不执行→平台泵拍才执行→停止边界显式失败+pending 不执行；LIT 退役后「链路健康电平」组覆盖持续失败 degraded/recovered 电平）✅；双轴 CLEAN+全套 0 警 0 错 ✅；候选纪律 ✅。

## 3. 双轴独立审查链（每轮两轴全新实例并行，零上下文，未合轴）

| 轮 | Standards 轴 | Spec 轴 | 处置 |
|---|---|---|---|
| R1 | **CLEAN**（0 硬伤；8 deferrable 具名） | **FINDINGS**（1 gap：回放第四态 transport-unavailable 无红测锚——验收「回放四态区分」只钉了三态） | F1 |
| R2 | **CLEAN**（F1 无新硬伤；4 deferrable 保留具名） | **CLEAN**（gap 闭环确认+全票面重扫 0 gap 0 deviation；Scope 七条/验收五条/定案对账/同权四条逐条摘证） | 闭环 |

- **F1（R1 后处置）**：① Gap1→`NetworkV3GroupDeferredProjections` (e) 块：直接门面+`NetworkV3ThrowingNetwork` 假运行时钉第四态（transport-unavailable 恰一条/stage 定位发送法/sessions 同投影/与前三态 token 互斥）；② `OwnerState` 注释与实现一致化；③ `OpenGeneration` hostStopped 复位注释具名（LIT EnsureOpen 重开先例；停止边界从不触 OpenGeneration，模块侧无 mutator）；④ `TickOnce` 泵失败行补 `diagnosticId=BUE-MT-006`；⑤ 票面定案码族登记 BUE-MT-006 + BUE-MT-GEN/CREATED（不入附录 B 拒绝码表，归 08 文档口径）。
- Standards deferrable（保留具名，不阻塞）：① `WithdrawPendingLocked` 锁内 O(n) 重排（停止/换代事件非热路径，容量 256 有界，保 FIFO）；② Pump 出队后同拍兄弟任务不复核失效（生产=宿主主线程单线程序列，Invalidate 与 Pump 无交错窗口；理论多线程窗口经视图无可变面可触发）；③ `SendTargets` 全执行失败+夹杂节流折叠 LocalTransportUnavailable（逐目标节流行仍在，无静默；聚合判断题，Spec 不判偏差）；④ BUE-MT-GEN/CREATED 命名码（非 nnn 拒绝码族，已在票面登记）。
- Spec R2 备注（非阻塞）：R1 TOCTOU 项（死代际健康账）经复核**不成立**——guard 触达全部在运行时 sync 锁内与 DropGeneration 串行。
- 审查链有效性：每轮两轴均新 spawn（`subagent_type` 显式 standards-reviewer/Spec-Reviewer），无跨轮实例续用。

## 4. 全套验证（终态留档）

- 全方案 Rebuild：0 错 0 警（`final-sln-rebuild.log`，14 工程输出）
- 7 个测试 exe 直跑全 PASS（`final-fullsuite-*.txt`：Contracts/Network/Placement/Settings/ClientUi/Release/Plugin）
- 增量：`final-increment.diff`（17 文件，+1396/−261，生产+测试+票面）
- 红绿证据：`red-plugin-network-run.log`（24）/`red-lit-sendhealth-run.log`（6）/`red-plugin-compile.log`/`red-contracts-compile.log`/`green-plugin-network-run.log`/`green-lit-sendhealth-run.log`（`.gitignore` 纪律：log/diff 磁盘归档不入库，报告+final-fullsuite txt 入库）

## 5. 冻结面变更清单（Minor 2.1 批次登记；SDK 条目归 DEV-V3-08 附录 A/B）

1. `NetworkSendResult.Throttled = 205`（加性枚举值；旧模块对未知结果安全降级纪律=SDK 要求条目）；
2. `IFeatureBootstrap.MainThread`（第 12 成员；矩阵行=本票后可用，未接线宿主 null 阶段基线）；
3. `IFeatureMainThread.Post(Action)`（恰一方法 fire-and-forget；最小行为面五条+执行异常隔离+禁自建泵入注释冻结）；
4. `MainThreadPostReason {None=0,CapacityExceeded=1,GenerationInvalid=2}` 值冻结+`MainThreadPostResult` 显式三元组；
5. `FeatureBootstrap` 11 参加性构造器（可选尾参，既有调用点不动，DEV-V2-21 先例）；
6. 诊断码族 BUE-NET-001..005（预算/degraded/recovered/入站异常/回放四态投影）+ BUE-MT-ACCEPT/001..006（附录 B 登记；BUE-MT-GEN/CREATED=宿主观察行不入拒绝码表）；
7. 平台数值冻结：预算 256/会话/2000ms 固定窗、dispatcher 容量 256+每拍 32、链路健康阈值 10（degraded/recovered 每 episode 各恰一条）；聚合扩展表（纯节流→Throttled；混合含节流→PartialFailure；既有组合不动）；
8. 入站回调线程=传输泵线程（SDK 冻结登记条目，文档落位归 08）+ Sessions established-only 边界说明（既有，08 重申）。

## 6. 移交注记

- SDK 契约文档三处（入站泵线程语义/dispatcher 用法与未知结果降级/预算与健康数值及码族）→ **DEV-V3-08** 附录 A/B（票面明文）。
- NoOp 全链 probe（Network→HostTick→Settings→Logger 顺序扩链）→ **DEV-V3-08**；本票=MainThreadAvailable/Posted 最小对照。
- 链路健康阈值用户可配置化 → **Settings 域**（DEV-V3-06+；本票只冻默认 10）。
- LIT 自有 `MainThreadDispatcher`（Tidy 私有泵+Cancel 补偿事务族）未迁移：平台最小行为面（fire-and-forget 无取消面）覆盖不了其 lease 补偿语义，属判断题具名——后续票若给平台补取消面再评估；本票「禁自建泵」约束对新代码生效（LIR 已示范消费形状）。
- 官方既有消费者证据保留：LIT 命名频道发送/LIR 请求响应/LHT 会话驱动组播全链回归绿（裁决八④）。
