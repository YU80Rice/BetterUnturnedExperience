# DEV-V3-03 结单报告 — BueLifecycle 统一状态投影与资源接线（统一状态机+TryTrack+只读查询+面板启停 seam）

- 日期：2026-09-10
- 票面：`.scratch/bue-v2-phase3-platform/issues/DEV-V3-03-bue-lifecycle.md`
- 状态：**resolved（双轴 R1 双 CLEAN 闭环）**
- 候选纪律：本票为 Phase-3 中间票——**不产正式候选 DLL、不更新 RELEASES、不授 CaseId**（spec 候选策略：01..08 中间构建=开发态内部基线；唯一 2.1 候选随 DEV-V3-09 授予）

## 1. 交付面（生产代码，12 文件 +1473/−68）

| 文件 | 内容 |
|---|---|
| `src/BetterUnturnedExperience.Contracts/ContractTypes.cs` | ①`IFeatureLifetime` 加性扩展：`FeatureStatusView CurrentStatus { get; }`（只读状态查询成员，命名本票定；Minor 2.1 加性；注释钉最小行为面=任何阶段可用/不抛/不 null/不可变快照/隔离停止后如实/仅限自身身份/无 mutator）；②`FeatureStatusView` 六参构造器+`NegotiatedFeatureView` 七参构造器（DEV-V2-21 加性构造器先例，getter 不变） |
| `src/BetterUnturnedExperience.Core/Registration/FeatureLifecycleRuntime.cs`（新） | 统一生命周期状态机（V3-T4 方案 A）：宿主驱动 01 票 owner-scoped 登记记录（State/LifecycleGeneration/OwnedResources/StopReason/Isolated/StopDiagnostic+新增 StateRevision/StateError）；五迁移 BeginStart（Discovered/Disabled/Isolated/Stopped→Starting，新代际分配，结果字段复位，StateRevision 永不复位）/CompleteStart（→Running）/Isolate（Starting·Running→Isolating→Isolated，RuntimeIsolated+错误+诊断，撤订阅与资源，不扩散不升级 CoreSafeMode）/BeginStop（→Stopping 带原因）/CompleteStop（→Stopped+UnsubscribeAll+逆序 Dispose）；每迁移 StateRevision++并浮出 BUE-LIFE-STATE 结构化行（reason/revision/generation）；TryTrack 管线=001 null→003 状态门（仅 Starting/Running 可登记，reason 区分 isolated/not-running/unknown）→004 旧代际视图→005 重复实例（不重复释放）→002 容量=64/功能/代际（本票定值），全显式 false+结构化诊断；登记成功 BUE-LIFE-ACCEPT；停止边界逆序释放单 Dispose 异常=006 隔离继续；生命周期视图（CurrentStatus 对 feature 答当前真相+TryTrack 代际绑定）；Dependencies=`FrozenCatalogDependencyView` 冻结目录只读查询（presence-only 投影：空能力串+0 版=存在性；非空能力/版本需求 fail-closed=false；TryGet 命中=Available+空能力集+目录修订号；非求解器）；诊断 sink 全在锁外（快照-清空-锁外回调纪律） |
| `src/BetterUnturnedExperience.Core/Registration/FeatureRegistrationRuntime.cs` | 记录补 `StateRevision`/`StateError`；内部访问器 `TryGetRecord`（记录=唯一事实载体；公开桥仍是窄结果面，不公开 session） |
| `src/BetterUnturnedExperience.Plugin/BueFeatureStartRuntime.cs` | StartCatalog 全面过状态机：工厂抛/null→Isolate(stage=factory)；Start 抛/not-started→Isolate(stage=start/start-result)；成功→CompleteStart+条目登记（Machine/Module/LifetimeView/Identity/Network/Stopped）；条目持有逐 StartCatalog 机器实例（无散装全局表）；`SetFeatureEnabled(FeatureId,bool)` 面板启停 command seam：disable=UserDisabled 三段停（BeginStop→module.Stop→CompleteStop，Stop 异常不跳过清理）显式 true/false+诊断行；enable=同工厂新代际重臂（BeginStart→factory→compose→Start→CompleteStart），非法状态/未知功能显式 false；Isolated 不自动重启仅此 seam 手动回 Starting；启停后不再收事件（UnsubscribeAll）/资源已撤；矩阵注释更新（Lifetime/Dependencies 自本票非 null） |
| `src/BetterUnturnedExperience.Lit/InventoryTidyModule.cs`+`Tidy/LitTidyNetService.cs` | 官方先行消费：模块捕获自身 `Lifetime` 视图（只读查询缝消费）+把真实入站网络订阅句柄经 TryTrack 交付宿主（NetService 暴露 `SubscriptionHandles` 只读快照；句柄 Dispose 幂等，与模块自身三段停双保险） |
| `src/BetterUnturnedExperience.NoOpFixture/NoOpFeaturePlugin.cs` | NoOp probe 扩展为生态契约侧对照：Start 消费 TryTrack（一资源）+只读查询（Start 期观察 Starting）+诚实 Started=true（R1 盘点「NoOp Start 返回 false 不练缝」空壳缺口闭合）；`ProbeRegistration`/`LastProbe` 观察缝（样例面，非 SDK 契约） |
| csproj×2 | Core 白名单+Plugin 嵌入清单（单 DLL 闭合）加新文件 |
| tests×2 | Plugin.Tests 新组 `AssertBueV3LifecycleProjection` 12 子组（矩阵接线侧/TryTrack 登记/逆序释放/停止不可再登记/隔离资源撤销/CoreSafeMode 不升级/容量与拒绝/面板启停/状态投影/目录能力查询/代际轴分离/官方先行消费锚/生态对照 NoOp）+`--bue-v3-lifecycle-red` 锚+探针夹具族（事件日志观察外显行为）；01 矩阵组更新（Lifetime/Dependencies 接线后非 null，Settings/Logger 仍 null）；Contracts.Tests DEV-V3-03 锚块（IFeatureLifetime 双成员/九态冻结/StopReason 值冻结/六成员投影/Has-TryGet 面） |

## 2. 红测先行（TDD 证据链）

1. **观测红·行为**（`red-plugin-lifecycle-run.log`，11 条）：Lifetime/Dependencies 接线侧 null×2、TryTrack 缝不可用×2、逆序释放×3、停止不可再登记×2、隔离资源撤销×2（CoreSafeMode 回归锚/EventRegistry 行/Settings-Logger 仍 null 行当时即绿=既有语义回归锚）。
2. **观测红·编译**（`red-plugin-compile.log`/`red-contracts-compile.log`）：CS1061 `CurrentStatus`×14、CS0117 `SetFeatureEnabled`×8、CS1061 LIT `Lifetime`×6、CS0117 NoOp probe 缝×7、CS0200 WiredModule×2。
3. **观测绿**（`green-plugin-fullsuite-run.log`+`final-fullsuite-*.txt`×7 全 PASS）。
4. 验收条件对照：假模块状态机各断言（Isolated 不扩散/逆序 Dispose/容量 65th 拒/UserDisabled 停+新代际启/隔离后查询可用/Has-TryGet 正反例）✅；矩阵两侧（接线前 null=01 票历史红侧+本票更新组红→绿；接线后可用=本票行为红）✅；官方先行消费锚=真实 LIT 经真 StartCatalog+真工厂装配（WiredModule 复位→工厂新实例）+真 UserDisabled 停止/新代际再启+真实 TryTrack 网络句柄（结构化登记行可观察）+再启用后 TidyCompleted 发布真实恢复 ✅；NoOp probe 生态契约侧对照 ✅。

## 3. 双轴独立审查链（每轮两轴全新实例并行，零上下文，未合轴）

| 轮 | Standards 轴 | Spec 轴 | 处置 |
|---|---|---|---|
| R1 | **CLEAN**（0 硬伤；5 deferrable） | **CLEAN**（0 gap 0 deviation；Scope 七条逐条对照票面/规格原文+实现证据；验收五条全兑现） | 闭环 |

- Spec 轴依据摘录：唯一状态投影无 setter/只读查询最小行为面全兑现/逆序 Dispose+006+容量 64/Dependencies presence-only fail-closed 非求解器/两代际轴分离+新代际旧代际失效+Isolated 仅显式 seam/CoreSafeMode 不升级红测/owner-scoped record 承载事实非散装表；LIT 真 seam 消费+NoOp 生态对照+diff 无 RELEASES/候选/CaseId。
- Standards 轴 deferrable（具名，不阻塞）：①`StartedModule.LifetimeView` 生产路径零读取（查询走 machine.CurrentStatus；04/06 接线可能消费，届时复用或删）；②静态表（started/Created/LastProbe）跨组不复位（当前套件顺序可绿；同 FeatureId 二次 StartCatalog 命中 TrackEntry 幂等分支）；③`SetFeatureEnabled` 失败路径 `entry.Stopped` 锁外写（宿主单线程现实无现伤）；④StartCatalog 与 SetFeatureEnabled 装配阶梯同构（可抽公共管线，后续票候选）；⑤`StopDiagnostic` 字段名复用为「末次迁移诊断」/`TryGetRecord` string 键/`NegotiatedFeatureView` 构造器未收空集——先例兼容判断题。
- 环境插曲（不影响链路有效性）：Spec-Reviewer 钉定模型 gpt-5.6-sol 空返回→改 grok-4.6 后 frontmatter `thoughtLevel: medium` 不被该模型支持→删行+应用重启后 R1 Spec 轴以全新实例闭环（全程无跨轮实例续用；记忆条 `spec-reviewer-model-broken-2026-09-10`）。

## 4. 全套验证（终态留档）

- 全方案 Rebuild：0 错 0 警（`final-sln-rebuild.log`）
- 7 个测试 exe 直跑全 PASS（`final-fullsuite-*.txt`：Contracts/Network/Placement/Settings/ClientUi/Release/Plugin）
- 增量：`final-increment.diff`（12 文件，+1473/−68，生产+测试）
- 红绿证据：`red-plugin-lifecycle-run.log`/`red-plugin-compile.log`/`red-contracts-compile.log`/`red-plugin-build.log`/`green-plugin-build.log`/`green-plugin-fullsuite-run.log`/`green-*-build.log`（`.gitignore` 纪律：log/diff 磁盘归档不入库，报告+final-fullsuite txt 入库）

## 5. 冻结面变更清单（Minor 2.1 批次登记；SDK 条目归 DEV-V3-08 附录 A/B）

1. `IFeatureLifetime.CurrentStatus` 只读状态查询成员（本票命名）；
2. `FeatureStatusView` 六参构造器（加性）；
3. `NegotiatedFeatureView` 七参构造器（加性）;
4. 诊断码 BUE-LIFE-STATE/ACCEPT/RELEASE/ISOLATE/001..006（附录 B 登记）；
5. Dependencies presence-only 能力投影规则（空能力串+0 版=存在性查询；非空能力 fail-closed）；
6. TryTrack 容量定值 64/功能/代际；
7. `SetFeatureEnabled` 面板 command seam（宿主内部组合面，非 SDK 契约面）。

## 6. 移交注记

- 面板命令 seam 的面板侧按钮接线随 DEV-V3-06 面板动态路由落地（本票=运行时 seam，票面口径）；面板显示读真实投影同票。
- `StartedModule.LifetimeView` 若 DEV-V3-04/06 无消费，建议届时删除（Standards deferrable ①）。
- NoOp probe 全链扩展（Network/Settings/Logger 等票后终态路径）归 DEV-V3-08。
