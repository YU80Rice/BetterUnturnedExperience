# DEV-V3-01 结单报告 — 注册桥与 Bootstrap 基线（官方身份白名单+十成员可用性矩阵+内部登记记录）

- 日期：2026-09-10
- 票面：`.scratch/bue-v2-phase3-platform/issues/DEV-V3-01-registration-bridge-bootstrap.md`
- 状态：**resolved（双轴 R2 双 CLEAN 闭环）**
- 候选纪律：本票为 Phase-3 中间票——**不产正式候选 DLL、不更新 RELEASES、不授 CaseId**（spec 候选策略：01..08 中间构建=开发态内部基线；唯一 2.1 候选随 DEV-V3-09 授予）

## 1. 交付面（生产代码）

| 文件 | 内容 |
|---|---|
| `src/BetterUnturnedExperience.Contracts/ContractTypes.cs` | `FeatureRegistrationReason.ReservedFeatureId = 206`（加性枚举值，200 段定义级拒绝块，诊断码 BUE-REG-010）；`FeatureScopeIdentity` 五参构造器（加性，DEV-V2-21「构造器加性入冻结面」先例，既有 getter 不变） |
| `src/BetterUnturnedExperience.Core/Registration/FeatureRegistrationRuntime.cs` | ① `OfficialFeatureIdentity`（internal）：保留段= `io.github.yu80rice.bue` 裸根+`io.github.yu80rice.bue.` 前缀（Ordinal），白名单七项=BII/LIT/LIR/LHT/network/network.v1compat/noop；不反射 caller、不读路径；② 判定顺序落票面原文：基础校验(004)→格式校验(005)→**保留段(010)**→合同版本(006)→ClientUi(008)→重复(007)；③ `SupportedContractMinor` 0→1（2.0 模块继续可注册）；④ `IsSupportedContract` 收紧为 `Major==2 && Minor<=1`（原谓词 `Major<2` 放行 V1 合同，与「宿主门槛 Major=2」矛盾，R1 Spec 发现后修复）；⑤ `FeatureRegistrationRecord` owner-scoped 登记记录替换裸 snapshot 字典：Registration（注册来源=受理工件快照）+RegistrationSource（"registration-bridge"）+State（Discovered 起）+LifecycleGeneration+OwnedResources+StopReason/Isolated/StopDiagnostic |
| `src/BetterUnturnedExperience.Plugin/BueFeatureStartRuntime.cs` | `StartCatalog` 组装绑定真实 `FeatureScopeIdentity`（Id/DefinitionSetId/DefinitionSetDigest 来自登记工件；FeatureVersion/CurrentSlug 注册面无数据源→null）；五 null 侧（Lifetime/Dependencies/MainThread/Settings/Logger）以注释钉矩阵口径 |

## 2. 红测先行（TDD 证据链）

1. **观测红**（`red-contracts-compile.log` + `red-plugin-run.log`）：
   - Contracts.Tests：CS0117 ×4——`ReservedFeatureId` 不存在，编译红；
   - Plugin.Tests：行为红——「the public bridge rejects a rogue reserved-segment identity with BUE-REG-010」（rogue 当时被接受）。
2. **观测红（Identity 缝，独立复现）**（`red-plugin-identity.log`）：仅 stash `BueFeatureStartRuntime.cs` 重建后，矩阵测试在「Identity binds the feature's own registration identity」断言失败——真实组装 `default(FeatureScopeIdentity)` 时 `Id.Value=null`。首轮红观察时该断言被同方法先抛的 rogue 断言遮蔽，故按 Fresh 证据纪律单独复现留档。
3. **实现**：上述三文件落地。
4. **观测绿**：Contracts.Tests / Plugin.Tests 双 PASS。
5. **R2 修复红**：`(1,0)` 拒绝断言先行观察红（原谓词放行 V1 合同），谓词收紧后转绿。
6. 票面验收断言逐条对应：
   - 逐码锚 BUE-REG-001..010：Reason+DiagnosticId 双断言（Contracts.Tests）。001..009 为既有正确行为的 green-on-arrival 钉（其中 009 为 catch-all fail-closed 路径首次被诊断码级锚定）；行为红集中在 010/白名单/顺序/契约门；
   - 白名单正反例+前缀边界：七项官方 id 全过、rogue 拒、裸根拒、`io.github.yu80rice.bue2.rogue`（兄弟反向域）豁免；
   - 判定顺序两锚：格式桶(005)先于保留段(010)、保留段(010)先于合同门(006)；
   - 矩阵两侧（Plugin.Tests 经真实 `StartCatalog` 组装）：五成员永非 null（Events/OwnedEvents/Network 引用非空+LifecycleGeneration≠0+Identity 绑定探测 id 与 DefinitionSetId）、四成员基线 null（Settings/Logger/Dependencies/Lifetime）；
   - 契约门：`(2,1)` 过、`(2,0)` 兼容过、`(2,2)`/`(1,0)`/`(3,0)` 拒；
   - 官方先行消费锚：BII（`Register()`）、LIT/LIR/LHT（`CreateRegistration()`+桥）、network 对（`CreateOfficialRegistrations()`+桥）全部经公共 `BueRuntimeHost` 断言 Accepted。

## 3. 双轴独立审查链（每轮两轴全新实例并行，零上下文，未合轴）

| 轮 | Standards 轴 | Spec 轴 | 处置 |
|---|---|---|---|
| R1 | **CLEAN**（1 deferrable-smell：MatrixProbeRegistration 死字段） | **FINDINGS**（2 gap + 1 deviation，无 blocking） | F1 |
| R2 | **CLEAN**（0 发现） | **CLEAN**（0 发现；递延归属与判断口径均核实成立） | 闭环 |

- R1 Spec ①（gap：SDK 逐码文档缺失）→ 不采纳为本票工作，**具名递延 DEV-V3-08**（spec「SDK 契约文档（V3-T9 → DEV-V3-08）」附录 B 总账；本票候选纪律禁对外文档）。R2 核实递延归属成立。
- R1 Spec ②（gap：FeatureId 格式文法校验缺失）→ **判断口径**：票面/规格的「格式校验」=既有工件校验桶（BUE-REG-004/005/008），两文件全文未定义 FeatureId 反向域名文法或对应拒绝码；新造文法属越界且无码可依。R2 核实口径成立。
- R1 Spec ③（deviation：`IsSupportedContract` 放行 Major=1）→ **成立，F1 修复**：谓词收紧为 `Major==2 && Minor<=1` + `(1,0)` 拒绝红测先行。008 复用同谓词行为一致（R2 双轴确认：Major=1 satellite 走 008 语义，合理无外溢）。
- F1 增量另含 Standards R1 deferrable 清理（MatrixProbeRegistration 死字段 featureId 删除）。

### 具名延期/判断口径（不阻塞，逐条列出）

1. **SDK 逐码文档**（BUE-REG-001..010 逐码处理建议+保留段合法/非法示例）→ DEV-V3-08 附录 B，本票不做。
2. **FeatureId 文法校验**：规格未定义文法与码，不做；未来若需要须先开决策票立码。
3. **MainThread 矩阵行**：成员归 DEV-V3-04 定名并接线（spec 澄清修订原文），本票基线以「未接线=null」文档口径呈现，无成员可钉。
4. **登记记录字段本票只立载体**：State=Discovered/Generation=0/OwnedResources 空/Stop 结果空，迁移与赋值归 DEV-V3-03（spec：记录为「后续生命周期票提供事实载体」）。按「只测外显行为」纪律，记录本身不做直接断言，其行为后果随 03 状态机落地而锚定——本票无对应红测属纪律内缺口，非静默跳过。
5. **NoOp 夹具身份入白名单**：`io.github.yu80rice.bue.noop` 为随主 DLL 发布的官方样例夹具；不改夹具身份（改身份波及 SDK 样例锚定与既有测试面，非本票范围）。
6. **Ordinal 判定**：白名单/保留段均为 Ordinal 字符串比较，大小写变体不落入保留段——与全域 FeatureId Ordinal 纪律一致（registration 字典 `StringComparer.Ordinal`）。
7. **Identity 的 FeatureVersion/CurrentSlug 为 null**：注册面无数据源；DEV-V3-08 附录 A 登记 Identity 保证时须如实记载字段可用性。

## 4. 全套验证（终态留档）

- 全方案 Rebuild：0 错 0 警（`final-sln-rebuild.log`）
- 7 个测试 exe 直跑全 PASS（`final-fullsuite-*.txt`：Contracts/Network/Placement/Settings/ClientUi/Plugin/Release）
- 证据：`red-contracts-compile.log` / `red-plugin-run.log` / `red-plugin-identity.log` / `final-increment.diff`（`.gitignore` 纪律：log/diff 磁盘归档不入库）

## 5. 身份与发布节奏

- 本票不产正式候选 DLL、不更新 RELEASES、不授 CaseId；生产 DLL 改动随下一张中间票累积，唯一 Phase-3 `2.1` 候选由 DEV-V3-09 三环境验收后按 output-review-loop 授予身份并加 RELEASES 行。
