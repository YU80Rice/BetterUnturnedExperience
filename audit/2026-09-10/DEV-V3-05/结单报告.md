# DEV-V3-05 结单报告 — 宿主时钟语义登记（零新增契约面+红测补齐+自节流登记）

- 日期：2026-09-10
- 票面：`.scratch/bue-v2-phase3-platform/issues/DEV-V3-05-host-tick.md`
- 状态：**resolved（双轴 R2 双 CLEAN 闭环；R1-Spec 因审查工具定义回滚延迟至应用重启后续链，全程登记见 §3）**
- 候选纪律：本票为 Phase-3 中间票——**不产正式候选 DLL、不更新 RELEASES、不授 CaseId**（spec 候选策略：01..08 中间构建=开发态内部基线；唯一 2.1 候选随 DEV-V3-09 授予）

## 1. 交付面（6 文件 +312/−1）

| 文件 | 内容 |
|---|---|
| `src/BetterUnturnedExperience.Core/Events/HostTickClock.cs` | 语义⑦登记补齐：时钟故障行补 `diagnosticId=BUE-CLOCK-001`（码族本票定案，宿主观察行不入附录 B 拒绝码表=BUE-MT-GEN 先例口径）；类注释登记暂停恢复语义（无拍=无产生/恢复拍=实际间隔/不追帧）+自节流=官方推荐模式（LHT 10Hz 先例）+禁自建第二泵 |
| `src/BetterUnturnedExperience.NoOpFixture/NoOpFeaturePlugin.cs` | T6 裁决③ HostTick 支线：ProbeState +六观察字段（HostTickSubscribed/HostTicksReceived/First/LastHostTickNumber/LastHostTickPhase/LastHostTickDeltaSeconds）；Start 经 `bootstrap.Events` 订阅 HostTick（null 容忍=矩阵阶段基线纪律），逐拍记录载荷三字段，不自拆订阅（停止边界归宿主 UnsubscribeAll） |
| `src/BetterUnturnedExperience.Plugin/BueFeatureStartRuntime.cs` | **红测揪出的生命周期接线缺陷修复**：`TrackEntry` existing 复用路径同步换绑 `Machine/Identity/Network`——旧实现留旧 machine，二次 StartCatalog（同进程 reload/测试复用）后 `BeginStop` 在旧 machine 上 `invalid-state` 被拒 → `CompleteStop/UnsubscribeAll` 跳过 → 新代际总线订阅泄漏过停止边界；生产单次 StartCatalog 形态不可达，属停止/热重载正确性面 |
| `tests/BetterUnturnedExperience.Plugin.Tests/Program.cs` | 新组 `AssertBueV3HostClockSemantics` 5 子组（暂停恢复语义/Tick 失败不扩散与结构化诊断/主线程构造性保证/官方先行消费锚 LHT 经真宿主时钟/生态对照 NoOp HostTick 支线）+`--bue-v3-clock-red` 锚+全套接线；八条语义既有锚（DEV-V2-19 假时钟钉死组/宿主身份保留组/DEV-V3-02 保留身份组/F-D 同帧去重组/停止注销组）维持回归 |
| `tests/BetterUnturnedExperience.Contracts.Tests/Program.cs` | DEV-V3-05 形状锚：HostTick 恰三属性（TickNumber/DeltaTime/Phase，无业务字段上时钟）+值类型+保留身份串冻结；TickPhase 恰一成员 Update=0（隐式加 Phase 必红）；零新增契约面锚（事件缝三视图恰一方法+IFeatureBootstrap 恰 11 成员不动） |
| 票面 Comments | SDK 登记条目建立：八条语义逐条+红测锚映射/自节流模式六条（LHT 先例/首拍 0/回拨已钳零/停止清态/禁第二泵）/同权三条/BUE-CLOCK-001 行格式/TrackEntry 缺陷具名/移交注记三处 |

## 2. 红测先行（TDD 证据链）

1. **观测红·行为 Round-1**（`red-plugin-clock-run.log` 1 条）：新组四子组只依赖既有 API——「Tick 失败不扩散与结构化诊断」红=BUE-CLOCK-001 token 缺失（既有失败行无码族具名）；暂停恢复/主线程构造/LHT 经真时钟三子组=既有行为回归钉（绿）。
2. **观测红·编译 Round-2**（`red-plugin-clock-compile.log` CS1061×8）：NoOp ProbeState 六新字段全引用。
3. **观测红·行为（全链首次合跑）**（`red-lifecycle-unsubscribe-run.log` 1 条）：生态对照子组在共享生产总线上暴露 TrackEntry 换绑缺陷（停止后 probe 仍收拍，DIAG 实证 seen=2→3/probe=2→3）——红测补齐的净发现，修复后转绿。
4. **观测绿**：`green-plugin-clock-run.log` 5 子组 ALL GREEN；`final-fullsuite-*.txt`×7 全 PASS；`final-sln-rebuild.log` 0 警 0 错（修复后全方案 Rebuild+全套复跑确认）。
5. 验收条件对照：fake-clock 补齐组各条先红后绿 ✅（红=BUE-CLOCK-001+TrackEntry 泄漏两处真实观测红；序单调/回拨钳零/首拍 0/异常不扩散既有面=既有 R1/R3 修复钉维持回归，票面 Comments 逐条映射）；SDK 登记条目建立 ✅（票面 Comments 正文，落档=08）；官方先行消费锚 ✅（LHT 既有全组回归+新真时钟经订阅缝锚）；双轴 CLEAN ✅（R1+R2 双轴全回全绿，见 §3）；全套 0 警 0 错 ✅；候选纪律 ✅。

## 3. 双轴独立审查链（每轮两轴全新实例并行，零上下文，未合轴）

| 轮 | Standards 轴 | Spec 轴 | 处置 |
|---|---|---|---|
| R1 | **CLEAN**（0 硬伤；2 deferrable 具名） | **CLEAN**（0 gap 0 deviation；首轮因工具故障 7 次空返回，应用重启后续链成功，逐条摘证） | F1（注释口误修正） |
| R2 | **CLEAN**（0 硬伤；1 deferrable 保留具名，未援 R1 独立判） | **CLEAN**（重点核对 1..7 全部独立重新取证；R2 义务=核实注释修正对齐+三红证据链一一对应） | 闭环 |

- Standards R1 deferrable：① `tests/BetterUnturnedExperience.Contracts.Tests/Program.cs` DEV-V3-05 块注释口误「twelve members」（断言=11 正确，与 ContractTypes 实数一致）——**F1 已修**（注释改 eleven，断言未动，全套 Contracts.Tests 复跑 PASS）；② `NoOpFeaturePlugin.cs` `hostTickSubscription` 字段仅作判空用途，局部变量即可——**保留具名**（持有句柄=生态作者真实形状，防「订阅未捕获即被 GC」误读；deferrable 不阻塞；R2 复判维持）。
- **环境插曲（如实登记）**：`spec-reviewer.md` 定义在本票实施期间被应用 UI/db 回写为坏组合（model=gpt-5.6-sol + thoughtLevel:medium，文件 mtime 19:54 实证；=DEV-V3-03 已修复过的两层故障复发）。R1 首发 7 次「零文本零工具调用零 usage」空返回（长提示词/精简提示词/任务卡文件三变体），同提供商 grok-4.6 的 standards-reviewer 同轮正常 → 定性=路由层，非提示词/通道。两层修复重落文件（grok-4.6+删 thoughtLevel）后**重启应用生效**，用户重启后「续 DEV-V3-05 双轴链」指令下 R1-Spec 全新实例续派成功返回 CLEAN。记忆条 [[spec-reviewer-model-broken-2026-09-10]] 已更新回滚复发+开工前核对 frontmatter 的纪律。
- 审查链有效性：R1/R2 每轮每轴均为新 spawn 全新实例（Standards 首尾 3 实例、Spec 有效 2 实例+故障期 7 次空返回不计轮），无跨轮实例续用；R1-Standards 结论在 R2 由全新实例复核（Fresh-instance 规则兑现）。

## 4. 全套验证（终态留档）

- 全方案 Rebuild：0 错 0 警（`final-sln-rebuild.log`，14 工程输出）
- 7 个测试 exe 直跑全 PASS（`final-fullsuite-*.txt`：Contracts/Network/Placement/Settings/ClientUi/Release/Plugin）
- 增量：`final-increment.diff`（6 文件 +312/−1，生产+测试+票面）/`changed-files.txt`
- 红绿证据：`red-plugin-clock-run.log`/`red-plugin-clock-compile.log`/`red-lifecycle-unsubscribe-run.log`/`green-plugin-clock-run.log`（`.gitignore` 纪律：log/diff 磁盘归档不入库，报告+final-fullsuite txt 入库）

## 5. 登记面（零新增契约面；SDK 条目归 DEV-V3-08 附录 A/B）

1. 八条语义登记正文（票面 Comments 一~八条，逐条含红测锚映射）；
2. 自节流=官方推荐模式条目（累计阈值/序号差值两模式+首拍 DeltaTime=0+回拨已钳零+停止清态+禁第二泵；LHT 10Hz=官方先例）；
3. 码族 BUE-CLOCK-001（时钟故障宿主观察行；行格式=event=host-tick result=failed errorType= message= diagnosticId=）；
4. 隐式扩展防线锚：TickPhase 恰一成员/HostTick 恰三属性/事件缝单方法/IFeatureBootstrap 恰 11 成员（任何隐式加面必红=「更多 Phase=显式契约登记扩展」的机器执行形态）；
5. TrackEntry 换绑修复=DEV-V3-03 停止边界语义的缺陷补齐（非新语义），08 附录 A「Lifecycle」节如实注记热重载正确性。

## 6. 移交注记

- 八条语义+自节流条目正文 → **DEV-V3-08** 附录 A「HostTick」节；BUE-CLOCK-001 行格式 → 08 附录 B 宿主观察行口径（与 BUE-MT-GEN/CREATED 同栏）。
- NoOp probe 全链（Network/Settings/Logger 顺序扩链）→ **DEV-V3-08**；本票=HostTick 支线最小形状。
- 可用性矩阵零行变化（本票零新增契约面）。
- 双轴链已闭环（R1+R2 双轴 CLEAN，见 §3），无遗留续派义务。
