# DEV-V3-07 结单报告：BueDiagnostics 统一诊断（双轴 R2 双 CLEAN）

日期：2026-09-11 ｜ 票：`.scratch/bue-v2-phase3-platform/issues/DEV-V3-07-bue-diagnostics.md`（Status=resolved）
基线：DEV-V3-01..06 全部 resolved（06=e2ae9ff）；候选纪律=不产候选 DLL/不更 RELEASES/不授 CaseId。

## 交付

- **零新增契约面**（T2 预授权兑现）：`IFeatureLogger` 三方法窄面与 `FeatureBootstrap.Logger` 形状未动；矩阵行「null→07 后可用」经组合根兑现；Contracts.Tests 形状锚钉死（三方法 void、参数 2/3/4、恰 11 成员零扩面）。
- Core 新 `Diagnostics/DiagnosticRuntime.cs`：FeatureLoggerView（代际绑+前缀纪律+消毒截断+异常不反噬）+有界摘要聚合器（(FeatureId,DiagnosticId) 聚合、容量 128、限频 30000ms、纯内存、T8 冻结摘要行形）。
- Plugin 新 `BueDiagnosticsRuntime.cs` 组合根（级别映射→BepInEx LogOutput 可见级、fallback、InvalidateOwner×5、PluginStopping ShutdownHost、Clear）。
- 收编双绑：生命周期机 sink+网络运行时 diagnosticSink 原行逐字不变、带码行进统一摘要（BUE-LIFE-*/BUE-NET-001/002/003）。
- 官方先行消费：LIT 启停两诊断行经注入 view（BUE-LIT-START/STOP）；NoOp 生态对照三方法支线。
- 码族本票定案：BUE-LOG-001 冒用拒写/002 无效标识符/003 容量溢出观察/004 边界拒写/005 故障隔离观察+CREATED/GEN 观察行（登记归 08 附录 B）。
- 矩阵三处既有 Logger==null 锚翻转+阶段基线 null 侧另锚；Plugin.Tests 新 10 子组+`--bue-v3-diagnostics-red`；全套注册。

## 红绿链

编译红 CS0234×42+CS1061×4（`red-plugin-diagnostics-compile.log`）→首轮行为红 8（`red-behavior-8-runs.log`；三根因=try 内早退吞发射/fault 格式空格/聚合器代际账 Clear 窗口）→ALL GREEN（`green-diagnostics-red-run.txt`）。红链期修红测自身缺陷两处具名（宿主 shutdown 断言须在 Recorder 复位前；G9 清 WiredModule=LIT ShuttingDown 单向旗标语义）。全套 7/7（`final-fullsuite-*.txt`）+全方案 Rebuild 0 警 0 错（`green-rebuild-sln.log`）。

## 双轴链（每轮全新实例）

- R1 Standards=FINDINGS：P1 OpenGeneration 不重臂 hostStopped（04 不同构，reload 后新代际全拒）；P2 单调钟未归一 Frequency；P2 fallback 双前缀；deferrable×5。R1 Spec=CLEAN（注记①T8 §2 枚举含 EventName 而冻结行不含→从冻结行；②收编面=spec 140 三缝，其余诊断行保留前票原缝）。
- F1：P1 重臂+新增 reload 红锚两断言；钟归一；fallback 去重；latch 入锁+决策合并单锁；ExtractToken 字段边界匹配。deferrable 3 条转修，2 条具名保留（收编一律 Info；string 标识面）。
- R2：**Standards CLEAN**（R1 七条逐条源码核实+增量终审无新违规，deferrable×2 维持）+ **Spec CLEAN**（独立重取证：Scope 五线/验收五盒/候选纪律全过，R1 两注记复验成立，F1 修复无规格偏差/超面）。

## 具名移交

附录 B 码表+附录 A Diagnostics 节=DEV-V3-08；NoOp 全链 probe+上架自检清单=DEV-V3-08；诊断实时视图/导出自动化=spec Out of Scope（不移交）。
