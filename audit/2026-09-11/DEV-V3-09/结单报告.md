# DEV-V3-09 结单报告（三环境验收与发布·唯一 2.1 候选）

Status: **进行中·停等人工实机验收 + 发布批准**（不 resolved、不关票、不动 RELEASES 批准态；依 auto-rm-test-sop 步 10）

## 0. 一句话

契约版本合批 2.1 文档登记 + 全套绿 + 双轴 CLEAN + 唯一候选已建成部署；三环境实机首验即抓到并修复一处发布级日志风暴（F1），修复后候选重验通过；剩网络缝完整验收/SP/P2P 需用户实机 + 发布批准。

## 1. 候选身份（开发态，批准后方定身）

| 项 | 值 |
|---|---|
| Candidate SHA-256 | `CE0D219179E036C3CACF6924E77DF2CE0A93A0A08B4BB73526AB46565A17E413` |
| 字节 | 601600 |
| 确定性 | 3× `-t:Rebuild` 逐字节一致（`candidate-sha256-final.txt`），全 exit 0 / 0 警 0 错 |
| CaseId | `DEV-V3-09-CANDIDATE-20260911` |
| 作废前序 | `471eb245…658fb0`（F1 修复前构建，不得用于采集，见 `candidate-sha256.txt` + FINDING） |
| 部署 | client `E:\Steam\steamapps\common\Unturned\BepInEx\plugins\` + U3DS `…\U3DS\BepInEx\plugins\`（替换 v7 `a1b339bf`）；双端 `certutil` = 候选值 |

## 2. 交付内容（本票源码/文档增量，均双轴 CLEAN）

### 2.1 契约版本合批生效（§7 文档登记）
- 正文 §7 版本活账：宿主支持契约版本 2.1（`SupportedContractMajor=2`/`SupportedContractMinor=1`，机器事实自 DEV-V3-01 成立）。
- 新增 `### 2.1` 单一批次加性小节：T2..T8 按票构成 + §C.3 加性四类理由（新枚举值/新矩阵成员/新可选面/新构造器；05/07 零新增契约面）+ 发布绑定。
- §C.1 翻转执行记录：区分「宿主支持 2.1（今）」vs「对外发布 2.1（本票候选验收+人工批准后随 RELEASES/publish 同提交翻转）」；附录定位句括注澄清「正文冻结=①–⑦基线原文，版本活账随发布票更新」。
- 破坏性变更为零：实现者侧 `IFeatureRegistration` 恰 4 属性不变；宿主侧接口加成员对 2.0 零破坏。

### 2.2 F1 日志风暴修复（实机发现，代码）
- 根因链：`InPlaceReloadModule.OnHostTick`(每帧) → `LirRepackNetwork.Drain()` 空队列仍 `seam.Post(DrainOnce)` → `MainThreadDispatcherRuntime` 每投递 emit 一行 Debug `result=posted`（U3DS 实机 ~63 行/秒）。DEV-V3-04 dispatcher 迁移新引入。
- 修复（LIR 内部，不触碰 2.1 dispatcher 冻结面）：`LirRepackDispatcher.HasPendingWork`（锁内 `requests.Count>0 || successes.Count>0`）；`Drain()` 接线分支 `if (!dispatcher.HasPendingWork) return;`。投递被拒仍下一拍重投（队未清=守卫仍真）。
- 红测 `NetworkV3GroupLirIdleDrainNoPost`：空闲 5 拍→0 投递；有工作→恰 1 投递+`dispatcher.Pump` 执行；排空后→转静默。先红（`InvalidOperationException: 日志风暴修复…`）后绿。
- 具名 deferrable（双轴不阻断）：测试夹具与既有消费锚同构（有意隔离）；空闲跳过 `AccumulateAndLogDiagnostics` 尾摘要延至下次活跃拍（xmldoc 已披露，功能私有非冻结 Post 契约）。

## 3. 双轴审查链（每轮全新实例）

- **轮 A（doc §7 翻转）**：R1 Standards CLEAN（2 deferrable）+ R1 Spec **1 deviation**（「既有 interface 成员一个未加」措辞把宿主侧 `IFeatureBootstrap`/`IFeatureLifetime` 加成员误纳入=假，仅实现者侧 `IFeatureRegistration` 未加；且漏 §C.3「新矩阵成员」类）→F（改写四类+实现者/宿主侧分离）→R2 双 CLEAN。
- **轮 B（F1 代码 + doc 定时）**：首派 R1 两实例均**网关连接超时作废**（api 连接/ TLS 断），同回合重派（全新实例）。重派 R1：Standards CLEAN；Spec CLEAN on F1 行为／**1 gap**（§7 勿将 2.1 断言为已发布，`RELEASES.md` 当前发布物仍 v8/2.0）→F（§7/C.1 分离「宿主支持」vs「对外发布」+点名批准前 RELEASES 仍 v8+同提交翻转）→R2 双 CLEAN。
- 全套测试 7/7 PASS；全方案 `-t:Rebuild` 0 警 0 错。

## 4. U3DS headless 自动实机证据（agent 代办，绑定 `ce0d2191`）

见 `auto-evidence/u3ds/`：
- `FINDING-lir-mt-post-flood-u3ds-20260911.log`（7595 行=红证据，修复前 `471eb245` 二进制仍风暴）
- `clean-boot-fixed-471eb245_to_ce0d2191-20260911.log` + `clean-boot-tally.txt`（修复后 `ce0d2191` 干净启动）
- 正例锚：`assembly-identity sha256=CE0D2191…`（精确匹配候选）；五官方功能 `accepted=True diagnosticId=BUE-REG-ACCEPT`（**2.0 模块在 2.1 宿主继续注册=非破坏回归实机成立**）；`diagnostic-summary`×11（统一诊断活）；`in-place-reload to=Running` + `module-start result=started` + dispatcher `generation-opened` + LHT `heartbeat`（证 host 泵在跑，`result=posted=0`=修复非停滞）；`[Error`=0。
- 环境事实：Steam 未登录 → `io.github.yu80rice.bue.network` + `v1compat` 传输不可初始化 → `event=feature-isolated error=ModuleStartFailed to=Isolated`（**生命周期隔离兜底实机正例**：单功能失败被隔离、其余功能续跑）；MT/settings `owner-invalidated reason=start-result`。**注**：此隔离=Steam 缺失所致非候选缺陷，网络缝完整判据需 Steam 登录后验（下节）。

## 5. 验收指引（给用户的编号步骤；agent 已部署双端 = `ce0d2191`）

> 前置：Steam 已登录（网络缝 + P2P 需在线）。部署已由 agent 完成，你只需游戏内操作 + 反馈异常。
1. **指纹核对**（可选）：`certutil -hashfile "E:\Steam\steamapps\common\Unturned\BepInEx\plugins\BetterUnturnedExperience.dll" SHA256` 应为 `ce0d2191…17e413`。
2. **单人 SP**：进游戏开一个单人/本地存档 → 过 BII 物品交互、LIT 背包整理、LIR 双击换弹/压弹、LHT（如触发）各用一遍；开 BUE 管理面板看四功能状态=正常、可启停、设置页可编辑保存；退到桌面用 UMM 导出日志。期望：功能无异常、无 BUE `[Error`、日志无 `[BUE-MT] …result=posted` 每秒刷屏。
3. **P2P 双端**：本机开一个游戏作房主 + 另一账号/机作客机进服 → 客机做 LIT 整理→LIR 压弹→LHT 尸潮，房主/客机各导出日志。期望：跨端功能一致、无异常、无风暴。
4. **U3DS 专用服（带 Steam）**：启 `U3DS`（`E:\Steam\steamapps\common\U3DS\Unturned.exe -NetTransport=SteamNetworking`）+ 客机连入 → 验网络功能（会话建立、LIT 代执行、LIR 跨端 repack、LHT /horde 广播、限流不刷）+ 无 `feature-isolated`（网络应不再因有 Steam 而隔离）。
5. **回传**：把三环境「无异常/异常」结论 + UMM 日志包给我（我回收归档绑 `ce0d2191`）。**放行发布请明确说「批准发布」**，我再同提交加 `RELEASES.md` 2.1 行 + 换 publish 交付包（v8 归档）+ 关单。

## 6. 停等边界

未 resolved。待：① U3DS 带 Steam 网络缝完整验收；② SP 实机；③ P2P 双端实机；④ 三环境全判据 + **人工发布批准** → 同提交：§7 发布态翻转闭环 + `RELEASES.md` 加 2.1 行（注明 Phase-3 平台缝 Minor 批次，绑本候选 SHA）+ `publish/` 交付包换新（DLL+本 SDK 文档+玩家手册+交付说明；v8 2.0 基线包归档）+ 票 resolved。
