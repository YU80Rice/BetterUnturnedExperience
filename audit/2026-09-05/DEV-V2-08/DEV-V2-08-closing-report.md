# DEV-V2-08 结单报告：LIT/LIR/LHT 生态迁移验证（三环境闭环）

> 工单：`.scratch/bue-v2-lmn-adoption/issues/DEV-V2-08-lit-lir-lht-migration-verification.md`
> 验证对象：归档验收候选构建（0.0.0）×3，绑定 RELEASES 行 6（`35670269…aef6`）+ LMN 权威 v5（`06d8a454…3055`）
> 证据：`audit/2026-09-05/evidence/DEV-V2-08-20260905/`（五环境包 + 三 case.md + 部署指纹）
> 交付报告/手册/身份：同目录 `Delivery-DEV-V2-08-ecosystem-kit-20260905.md` / `DEV-V2-08-ecosystem-handbook.md` / `identity-sha256.txt`

## 1. 验收条件裁决

| 票面验收条件 | 裁决 | 证据 |
|---|---|---|
| ① 三插件在 BUE 网络模块下实机收发正常（或记录明确缺陷+修复票） | **满足**——三插件 × 三环境全过，零 V2 迁移缺陷 | 三 case.md（§2） |
| ② 迁移验证闭环：V1 兼容层「已知生态」维度验证义务解除（T4 Q4） | **满足**——已知生态 3/3 全部以 LMN V2 命名频道在 BUE 接管态下实机验证；V1 兼容长期保留理由收敛至「未知第三方」 | 本报告 §2 + T8 盘点口径 |
| ③ 证据/日志归档 | **满足** | `evidence/DEV-V2-08-20260905/` 五环境包 + case.md ×3 |

## 2. 三环境结果矩阵

| CaseId | SP（09-05 夜） | P2P（09-06 晨） | U3DS（09-06 上午） |
|---|---|---|---|
| `DEV-V2-08-LIT-20260905` | 完整整理链多轮（回环），用户确认物品实际重排 | 跨端请求-响应闭环（客机 RequestTidy ×3→主机提交 ×4 含主机自 1，reqId 对齐） | 专用服务器跨端闭环（reqId=1 对齐） |
| `DEV-V2-08-LIR-20260905` | 三次双击均见成功 toast（SP 本地事务；rejected=1=冷却防护） | 主机队列派发 ×2 / 客机回包 ×1（差额=NoChange 设计语义） | U3DS 派发 ×2 / 客户端回包 ×2 |
| `DEV-V2-08-LHT-20260905` | 意外完整生命周期：爆发→广播 Update seq=1..63→Clear（服务器侧） | **恰好一次实锤：主机广播 Update ×10 = 客机收到 ×10（seq=1..10 连续无重复，不可靠通道 1:1）+ Clear 对齐**；客机玩家放置信标 | U3DS 广播 ×16 = 客户端收到 ×16（1:1 无重复键）；客户端先离场故无收到 Clear（P2P 轮已实锤 Clear 接收） |

**会话门（每环境每端）**：`takeover-patch result=installed` 在场 + `lmn2-frame-release result=released decision=lmn-native-dispatch` 恰一条 + `lmn2-delegate` 全程零出现——BUE 接管决策核在全部五端行使决策，无回归。**零误报**：全部五包 0 条 `BUE 错误`；命中均为插件自身防护（LIT RateLimit 请求过快、退出 BeginQuiesce/注销线）。

## 3. 人工验收记录（用户原话留档）

- SP 轮：「按我人工体验，三个插件的功能均无异常可使用」；LIT「确实看到了重排」；LIR「双击了三次，均看到了一键压弹成功」
- P2P 轮：「双端功能无异常，客机放置的信标后，主机也可以收到尸潮播报」
- U3DS 轮：「人工验证 功能无异常」

## 4. 具名事项（不阻断，均留档）

1. **O-LIT-1**：用户认为 LIT 排列算法质量有问题（拍板「后续再说」）——非 V2 迁移缺陷（网络链完整），记官方纳入票议程。
2. **缺口具名（Spec R1-H1，交付报告 §6）**：票面「不再需要独立 LMN DLL，走 BUE 注册/生命周期/隔离路径」与「不做：不修改三个插件源码」票内互斥，本票不可验证——官方纳入终态（BueNetworkApi 重写 + 并入 BUE 单 DLL）另立票承接。
3. **截图/时间窗 gap**：各轮截图与 Get-Date 时间窗截图未采集，以包时间戳 + 日志锚 + 用户口头确认代偿（P2P/U3DS 的 1:1 计数与 reqId 对齐为客观日志证据）。
4. LHT U3DS 轮客户端先离场，`收到 Clear` 归 P2P 轮证据。

## 5. 交付物与审查链

- kit 五件（BUE=行 6 复用、LMN 权威 v5、LIT/LIR/LHT 重建件确定性两轮一致）；BUE 与三插件源码零修改；归档区仅 junction+Libs 供给（交付报告 §5）。
- 文档双轴审查 R1→R8 双 CLEAN（含 agent 自查纠出 DEV-V2-11 委托口径→DEV-V2-12 release 口径的材料性修正，审查史见交付报告 §7）。
- 提交链：`4b48a31`（kit 交付）→ `dc0ad9c`（R5-R8 闭环）→ `88d4f34`（SP 轮）→ `cee4a61`（P2P 轮）→ 本结单提交。

## 6. 后续（不在本票）

1. **建议建票「三插件官方纳入实施」**：按 BueNetworkApi 重写三插件网络层并入 BUE 单 DLL（含 O-LIT-1 排列算法质量复议）；建票待用户拍板。
2. 三插件正式版本号授予（各 CHANGELOG 约定验收通过后编号 1.0.0）随官方纳入票决策。
