# DEV-V2-24 T7 C4 case·T7-4 探针 A 变体(001 正向半)——前提证伪 finding,边界事实处置(v7,2026-09-09 14:02,UMM 包 140253)
- 部署=指纹 deploy-fingerprint-c4.txt(候选原名+BueSameAsmProbe-ABefore,GUID 序在 BUE 前、文件名序在 BUE 后)
- **手册 C4 期望未被满足(001 双证均未出现)**:日志 PLATFORM-001=0;面板无红色状态行(用户截图 c4-panel-no-redline-20260909.png,面板状态健康:目录/详情/收藏齐,非渲染故障)
- **根因(与 C2 同机制,事实钉死)**:BepInEx 类型扫描段(:13,先于一切插件 Awake)即撞 Mono LoadFrom 身份折叠——扫描按文件名序先装载 BetterUnturnedExperience.dll(身份入 AppDomain),处理探针文件时 Cecil 元数据虽见 `BueSameAsmProbe.SameAsmProbe`,反射解析 `Assembly.LoadFrom(探针路径)` 按身份返回**已加载的 BUE 副本**→探针类型/特性解析不到→`Skipping over type … as no metadata attribute is specified` 跳过。**探针程序集从未进入 AppDomain**(Chainloader 仅列 Loading [SteamP2PFriends]/[Better Unturned Experience],[PROBE] awake 零条)。
- **判定=非诊断漏报**:AppDomain 中确实只存在一个 BetterUnturnedExperience 程序集——自检冻结契约(DEV-V2-23:只扫已进入 AppDomain 者,零文件 IO)下 001 无从触发=契约边界按设计工作。被证伪的是手册 C4 的**前提**(「副本程序集先于 BUE Awake 进入 AppDomain」)——BepInEx/Mono 标准装载路径上,同程序集名第二副本**永远到不了 Awake**(扫描段即被折叠)。GUID 序(ABefore 的 aaprobe<bue)不起作用:扫描序按文件名,BUE 文件名恒先于探针被装载并赢下身份竞速。
- **T7-4 双证处置**:红测半 CLEAN 维持(DEV-V2-23 `--bue-v2-platform-red`,真实 AppDomain 注入两副本=检测逻辑已证);实机半=**引擎层身份折叠事实**(副本被挡在 AppDomain 之外,001 为非标准装载路径的兜底网)——防双装三层图景成立:引擎折叠(标准路径上游阻断)→001(AppDomain 内检测,红测已证)→建议性处置。**不改代码**(把扫描面改成磁盘 IO 将违反冻结的零文件 IO 设计);SDK 文档 §6/§8 实机事实附注随关票文档轮落。
- C4 step-4 文件完整性 ✓:探针 DLL 全程原封(4096B,sha256=30b93000…d7c54a 不变);采后已摘除探针,本机恢复配置 A 终态(候选 v7+SPF)
- 完整日志=logoutput-v7-t7-c4-20260909-140253.log
