# DEV-V5-08 探针构建 #3（非候选，不授身份）
内容 = 候选 v2 d7549806 + ExecuteRepackFor 三处 [V508-PROBE3] 诊断（窗拒绝/Committed total=0/静默 outcome）
部署 = SP 客户端安装目录（P2P 双端共用同目录）；U3DS 保持候选 v2
用途 = 判定客机双击 R「有 CD 无效果」= 配置无可压 vs 跨端事务缺陷
