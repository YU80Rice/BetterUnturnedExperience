# DEV-V5-08 结单报告（第五阶段三环境验收·唯一对外候选）

Status: **RESOLVED·已关单**（2026-09-17 用户原话「批准」→ RELEASES 行 14 当前发布物 + `publish/第五阶段-正式交付版本/` 四件套 + 本票 resolved）。

## 0. 一句话

01..07 关闭后打出阶段唯一对外候选；SP 轮1 抓到身份链 NRE（v1 作废）→ 技能窗/自动轮跨端两轮修复（v2/v3 作废）→ U3DS 回放闸+容器按钮 setter（v4）。用户确认 U3DS「既有整理也有自动压弹」。契约仍 2.1。

## 1. 候选身份（开发态，批准后方定身）

| 项 | 值 |
|---|---|
| **当前候选 SHA-256（v4）** | `F334657ABA2C6AD7CAC8E39E7A36C19FB38DCC19E60DCC575BF20D9777380D45` |
| 字节 | 726016 |
| 确定性 | 3× `-t:Rebuild` 逐字节一致（`u3ds-fix-rebuild-1..3.txt`） |
| **作废前序** | v3 `589300E2…915A`（F10/F11）→ v2 `D7549806…0D45`（F8/F9）→ v1 `BB33C2DD…4E16B`（F1/F2）。探针 dc948b54/e5ef12ac/f4bdd440/51c2dc94/1c9bba9a 非候选 |
| CaseId | `DEV-V5-08-CANDIDATE-20260916` |
| 契约 | 仍 2.1（`contracts-diff.txt` 相对行 13 基线空；修复全 internal） |
| 部署 | SP + U3DS 在位哈希 = v4 |

## 2. 静态门

- 01..07 resolved；`src/`/`tests/` 工作树仅本票修复增量。
- 全套 7/7 + 六门禁（NoUiTokens-Contracts = V2-02 基线命中如实双记）。
- 发布工件轮双轴 R1→R3 CLEAN；修复轮 FIX-R1→R3 CLEAN（v2 身份）。v3/v4 为实机阻断修复，红测组随增量扩展（成交才开窗 / wireId=0 / HostInitiated 闸）。

## 3. 实机缺陷与修复

- **F1/F2** `SteamPlayerID` 自定义 `==` 无判空 → `is null` + IL 守卫。
- **F8** 0 级窗放行即武装 → 仅权威成交 >0 发后武装。
- **F9** 自动轮回包被客机 pending 丢掉 → wireId=0。
- **F10** U3DS 自动轮 `NextRequestId` < 客机手动 id → `TryAcquireHostInitiated` 跳过回放比较、仍吃 1.5s 闸。
- **F11** 容器按钮对接口 `IsVisible` SetValue NRE → 实现类型属性 setter。

## 4. 三环境抽检

- **SP**：整理/容器/入包恢复/转移恢复/HUD/技能 0–2/生命周期；等级账 `DiDATUT|2`、`一杯苦艾酒|1`。
- **P2P**：双端 v3/v4 身份；跨端整理/恢复/升级定向；自动轮 `RepackSuccess(reqId=0,total=12,auto=1)`。0 级真机因角色已升无法回退，红测钉成交才开窗；用户确认双击压弹与自动压弹可用。
- **U3DS**：headless 负面（不武装 HUD/分区/整理钮，LIT/LIR Running）；权威入包恢复+容器提交+压弹；v4 用户确认整理按钮+自动压弹；`RepackSuccess(reqId=0,total=6,auto=1)`。

## 5. 缺口台账

26 项见 `gap-ledger.md` 终态表。定性 5 项均注明替代证据。残留观察：G07-3 E1 askAward 异常不可注入。

## 6. 关单流程（批准后同一次提交）

用户原话批准 → `RELEASES.md` 行 14 当前发布物 + 行 13 改已取代 + `publish/第五阶段-正式交付版本/` 四件套（DLL+玩家手册+SDK 2.1+交付说明）+ 本票 resolved。
