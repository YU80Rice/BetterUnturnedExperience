DEV-V5-07 红测先行证据链（2026-09-16，/implement 会话）
============================================================

1. 编译红（红测先行、实现未存在时）
   - 命令：MSBuild BetterUnturnedExperience.sln -t:Rebuild -p:Configuration=Release
   - 结果：EXIT=1；DevV5ReloadSkillTests.cs 对未实现类型引用 CS0246 ×7：
     IReloadSkillPersistence / ReloadSkillRecord(×3) / ILirSkillHooks /
     ReloadSkillUpgradeDecision(×2)
   - 原始日志：build-red-v507.log（会话工作区）

2. 行为绿（实现落地后）
   - 终态 Rebuild：EXIT=0，error CS=0、warning CS=0（build-final-v507.log）
   - V5-07 收集器：--bue-v5-07-reload-skill-red →
     "DEV-V5-07 reload-skill collection: ALL GREEN (0 failures) —
      groups: 常量/冷却/升级/自动压弹/官方先行与表面/生命周期"
   - 7 套件直跑全 EXIT=0（Plugin/ClientUi/Contracts/Network/Placement/Release/Settings），
     既有 DEV-V5-03/04/05/06 组 PASS 无回归。

3. 突变证红（各改源码→Rebuild 核验 EXIT=0→跑红收集器→还原；构建失败不计数——
   06 假证据教训显式核验）：
   - M1 ExtraCooldownSeconds(0)=8→恒0 → 红 7 条（额外冷却/合并窗/拒绝不推进窗/新档窗 等）
   - M2 合并窗少加技术闸 1.5s → 红 1 条（剩余秒=合并窗公式）
   - M3 LevelDrift 判据 !=current+1 → !=current（允许跳级/重复请求）→ 红 8 条
   - M4 删除经验校验分支 → 红 1 条（经验差1不足=零扣零改）
   - M5 落盘失败删回滚行 → 红 1 条（TryCommitUpgrade 回滚+显式错误）
   - M6 MaxSkillLevel 2→3（引入三级）→ 红 12 条（常量/三级拒绝/账 fail-closed/满级 等）
   - M7（首版 if(false) 触发 CS0162 构建失败=不计证据，自纠）指纹匹配恒真 →
     红 1 条（切枪取消到点轮）
   - M8 排轮忽略 wasAuto → 红 1 条（到点只压一轮）
   全部还原后终态 Rebuild 0/0 + 全套件复绿（见 2）。

4. 门禁（6 条全绿）
   - Verify-DeveloperHandbook PASS（手册层+README+6 横幅）
   - Verify-RefreshModelDeduped PASS
   - Verify-SpecV4R9Ingested PASS
   - Verify-TestFixturesTracked PASS
   - Verify-TestRunnerHostDllsProvisioned PASS
   - Verify-NoUiTokens：Core PASS（30 文件）；Contracts 命中=V2-02 既有注释基线
     （ContractTypes.cs:425 禁词自述行，git diff 对本票 = 0 改动，逐字与基线相同）
