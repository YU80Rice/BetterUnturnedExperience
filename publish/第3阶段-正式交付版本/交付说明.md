# BUE · Phase-3 正式交付说明（生态开发者平台能力 · 对外契约 2.1）

- 交付日期：2026-09-11
- 定位：Better Unturned Experience（BUE）第三阶段（Phase-3）收官交付物——生态开发者平台能力定界与接线。仍为**单 DLL 交付形态**（四官方功能全并入，平台服务缝内嵌于主 DLL），无需独立分包或外部 loader。
- **对外契约版本 = 2.1**（宿主注册门槛 `SupportedContractMajor = 2`、`SupportedContractMinor = 1`）：**破坏性变更为零**，既有 2.0 生态模块**零重编译继续注册、继续运行**。
- 批准链：DEV-V3-01..08 红绿 TDD + 双轴独立审查逐票 CLEAN → DEV-V3-09 唯一 2.1 候选 → **三环境实机验收全判据通过（SP / P2P 双端 / U3DS+Steam，多端 `assembly-identity` = CE0D2191… 实机核验一致）** → **用户人工发布批准（2026-09-11，原话「批准发布！」）** → `audit/RELEASES.md` 行 11 **当前发布物**。

## 交付物清单

| 文件 | 说明 |
|---|---|
| `BetterUnturnedExperience.dll` | 唯一交付二进制（客户端与 U3DS 服务器同一份） |
| `BetterUnturnedExperience-Player-Handbook.md` | 玩家安装/升级/故障自查手册（副本；权威源在仓库 `docs/`） |
| `BetterUnturnedExperience-SDK-Assembly-Identity.md` | **开发者契约与 SDK 引用（2.1 附录版）**——正文八节（程序集身份冻结面 / 编译期引用 / 防双装 / 契约版本演化 ①..⑦ + **2.1 合批小节** / 实机验证清单）**+ 新增附录 A（平台服务参考七节）· 附录 B（诊断与身份码表）· 附录 C（契约版本与迁移 + 四条件门禁 + §C.6 生态 DLL 上架前自检清单 11 项）**。副本，唯一事实源随仓库 `docs/sdk/` 演进 |
| `交付说明.md` | 本文件——身份数字与证据链 |

## 版本身份

- **DLL SHA-256**：`CE0D219179E036C3CACF6924E77DF2CE0A93A0A08B4BB73526AB46565A17E413`
- 字节数：601600
- CaseId：`DEV-V3-09-CANDIDATE-20260911`
- 确定性：同日三轮 `-t:Rebuild` 逐字节一致，0 警告 0 错误（`audit/2026-09-11/DEV-V3-09/candidate-sha256-final.txt`）
- 源基线：`1da6d04`（DEV-V3-09 F1 修复 + §7 中间提交）；关单提交 = 本交付所在提交。
- 前序候选 `471eb245…658fb0` 因 F1 日志风暴修复作废，不得用于采集。
- 启动日志身份锚行：`event=assembly-identity path=…\BetterUnturnedExperience.dll sha256=CE0D2191…`——报问题时请附上该行。

## 覆盖面（Phase-3 工单 DEV-V3-01..09 全部 resolved）

- **八平台缝**（官方与生态同权、同一注册/生命周期路径）：
  - 01 注册桥 Admission（官方身份白名单 `BUE-REG-010` + Bootstrap 可用性矩阵）
  - 02 功能事件总线（事件类型归属登记 `IFeatureEventRegistry` + 路由归属不变量）
  - 03 统一生命周期（`FeatureState` 状态投影 + `IFeatureLifetime` 只读查询 + `TryTrack` 逆序资源释放 + 两代际轴隔离）
  - 04 网络（每会话发送预算 `NetworkSendResult.Throttled` + 链路健康电平 + 统一主线程 `IFeatureMainThread` + 入站异常诊断）
  - 05 宿主时钟（HostTick 八条语义登记，零新增契约面）
  - 06 设置（`bootstrap.Settings` 注入 + 面板目录动态路由 + 双 scope 权威门 + 乐观并发）
  - 07 诊断（`IFeatureLogger` 接线 + 有界诊断摘要 + `BUE-*` 前缀纪律）
  - 08 SDK 契约文档附录总装 + NoOp 统一生态契约 probe + 上架自检清单
- **F1 发布级修复（09 三环境实机首验发现）**：LIR→平台 dispatcher 每帧空投导致 `LogOutput.log` ~63 行/秒日志风暴；修复后按真实跨端负载投递（U3DS=2 / P2P=1 / SP=0，与压弹次数成比例），空闲不投。
- **三环境实机验收**：单人（SP）/ Steam 好友联机双端（P2P）/ U3DS 独立服务器（带 Steam 跨端），四官方功能 + 2.1 平台缝全锚通过。
- **兼容**：独立 LMN 共存语义、防双装边界随玩家手册；`io.github.yu80rice.betterunturnedexperience` GUID / 程序集名冻结不变。

## 生态作者接入

读 `BetterUnturnedExperience-SDK-Assembly-Identity.md`（正文八节 + 附录 A/B/C），对照活样板 `NoOpFixture` 的真实代码；`[BepInDependency]` 绑 GUID `io.github.yu80rice.betterunturnedexperience`（HardDependency），编译期引用本主 DLL 且 `CopyLocal=false`（禁捆绑）；发布前逐条走 §C.6 十一项自检清单。

## 具名递延

- **面板原生启停按钮 UI**：功能启停已可经宿主 command adapter 使用；原生管理面板按钮 UI 另立票（走 ClientUi satellite），不随 2.1 交付。
