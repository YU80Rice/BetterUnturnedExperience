# BUE · Phase-5 正式交付说明（现有官方功能优化 · 对外契约仍 2.1）

- 交付日期：2026-09-17
- 定位：Better Unturned Experience（BUE）第五阶段（Phase-5）收官交付物——**现有官方功能优化**：行带整理、容器会话整理、入包恢复、快速转移恢复、弹药后备 HUD、换弹技能 0～2。仍为**单 DLL 交付形态**。
- **对外契约版本 = 2.1（不变）**：Phase-5 八张票**零公开成员扩面**（全部 internal），2.0/2.1 生态模块零重编译继续注册、继续运行。
- 批准链：DEV-V5-01..07 红绿 TDD + 双轴独立审查逐票 CLEAN → DEV-V5-08 三环境验收（发布工件轮 R1→R3 CLEAN；实机修复 F1–F11 红测先行）→ **用户人工发布批准（2026-09-17，原话「批准」；U3DS「成了，现在既有整理，也有自动压弹」）** → `audit/RELEASES.md` 行 14 **当前发布物**。

## 交付物清单

| 文件 | 说明 |
|---|---|
| `BetterUnturnedExperience.dll` | 唯一交付二进制（客户端与 U3DS 服务器同一份） |
| `BetterUnturnedExperience-Player-Handbook.md` | 玩家安装/升级/故障自查手册（副本；权威源在仓库 `docs/`） |
| `BetterUnturnedExperience-SDK-Assembly-Identity.md` | **开发者契约与 SDK 引用（2.1 附录版）**——本阶段未变更。副本，唯一事实源随仓库 `docs/sdk/` 演进 |
| `交付说明.md` | 本文件——身份数字与证据链 |

## 版本身份

- **DLL SHA-256**：`F334657ABA2C6AD7CAC8E39E7A36C19FB38DCC19E60DCC575BF20D9777380D45`
- 字节数：726016
- CaseId：`DEV-V5-08-CANDIDATE-20260916`
- 确定性：三轮 `-t:Rebuild` 逐字节一致，0 警告 0 错误（`audit/2026-09-16/DEV-V5-08/u3ds-fix-rebuild-1..3.txt`）
- 源基线：01..07 + 08 实机修复链 F1–F11；关单提交 = 本交付所在提交。
- **前序候选作废**（不得用于采集）：v3 `589300E2…915A` / v2 `D7549806…0D45` / v1 `BB33C2DD…4E16B`；探针构建非候选。
- 启动日志身份锚行：`event=assembly-identity path=…\BetterUnturnedExperience.dll sha256=F334657A…`——报问题时请附上该行。

## 覆盖面（Phase-5 工单 DEV-V5-01..08 全部 resolved）

- **01 开发手册**：`docs/developer/` 一图三短章，不复制 SDK 码表。
- **02 行带排版**：用途标签分段、空位右下；旧三模式退役。
- **03 容器会话整理**：世界箱/授权后备箱标题栏一颗「整理」；失败中文原因、零修改。
- **04 入包恢复**：拾取/合成碎洞失败后五页重排再试；成功不触发压弹。
- **05 快速转移恢复**：Ctrl+右键碎洞只整理接收侧再试该件。
- **06 弹药后备 HUD**：「备匣 N · 备弹 M」。
- **07 换弹技能 0～2**：U 菜单战斗区下方分区；0 级额外冷却、1 级取消额外段、2 级成功后再自动压一轮（8s，一轮为限）。
- **08 三环境验收**：SP / P2P / U3DS；U3DS 不画客户端表面，权威行为真实。

## 实机修复链（08 验收发现，均红测先行）

- F1/F2：`SteamPlayerID` 自定义 `==` 无判空 → `is null` + IL 守卫。
- F8：0 级技能窗仅权威成交 >0 发后武装。
- F9：2 级自动轮回包 `requestId=0`，客机不走待确认表。
- F10：U3DS 自动轮走主机发起闸（跳过客户端 id 回放比较，仍吃 1.5s 技术闸）。
- F11：容器整理按钮 `IsVisible` 改实现类型 setter。

## 生态作者接入

读 `BetterUnturnedExperience-SDK-Assembly-Identity.md`（2.1 附录版，本阶段未变更）；对照活样板 `NoOpFixture` 与 `docs/developer/`。`[BepInDependency]` 绑 GUID `io.github.yu80rice.betterunturnedexperience`（HardDependency），编译期引用本主 DLL 且 `CopyLocal=false`。

## 具名递延（不随本交付）

- 完整超限供弹、装备栏锁定、工坊虚拟容器整理。
- 整理排序观感微调（用户裁定不阻断）。
- 技能分区原版图标样式（规格未承诺）。
- E1 `askAward` 退款异常边缘（不可注入）。
- 后台循环自动压弹（用户新设想，规格冻结为一轮为限）。
- SPF ResourceObs / P2P-UI 日志：SPF 域另案。
