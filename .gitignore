# ==== git 基线防护栏(2026-09-05 基线补录 chore 添加)====
# 纪律:候选 DLL 只经 audit/ 归档 + RELEASES.md 登记;bin/obj 与日志永远不入库。
# 注:V1 时代已误入库的历史 audit log/DLL(约 329 个)保持已跟踪原状,不追溯清理;
#     本文件只约束今后的未跟踪文件。

# ---- 构建输出 ----
[Bb]in/
[Oo]bj/

# ---- 二进制构建产物 ----
*.dll
*.exe
*.pdb

# ---- 正式交付包例外(2026-09-09 用户拍板「V2 第二阶段 v8 正式交付入库」)----
# publish/ 下的交付 DLL=正式发布物(身份以 RELEASES 行 10 + 包内交付说明.md 哈希为准),强制入库;
# 其余 *.dll 禁令不变——候选 DLL 仍只经 audit/ 磁盘归档 + RELEASES.md 登记。
!publish/**/*.dll

# ---- 构建工具 vendored(DEV-V6-02F 发布期合并)----
# tools/ILRepack = 发布期合并工具(vendored, 离线确定性构建的前提; 身份哈希在案
# audit/2026-09-19/DEV-V6-02F/)。例外仅指工具本体, 合并产物 DLL 仍被 *.dll 禁令吞掉。
!tools/ILRepack/*

# ---- 原型 vendored 运行时(prototypes/.video-deps = OpenCV/numpy,可重建,>100MB)----
.video-deps/
__pycache__/
*.pyc

# ---- 过程物：本地工单与规格（ADR 0004：公开树不含过程物）----
# 工作副本与 agent 跟踪器仍读这里；公开 clone 不带票面。
.scratch/

# ---- audit：公开树只留发布台账；日期目录证据留在工作副本 ----
audit/**
!audit/RELEASES.md
audit/**/*.zip
audit/**/*.diff

# ---- 日志(全局;V1 时代已跟踪的历史 audit log 保持原状)----
# 边界:测试夹具须随 Clone 入库时改用 .log.txt 等不被本规则吞掉的后缀(POST-P4-02);
#      兜底门禁=eng/Verify-TestFixturesTracked.ps1(CopyToOutput 夹具必须存在+未被忽略+已跟踪)。
*.log

# ---- MSBuild 二进制日志(.flp;与 *.log 同族,构建证据留在磁盘/audit 转录不入库)----
*.flp

# ---- 宣传片工作区（ADR 0004：不进公开树）----
promo-video/

# ---- IDE ----
.vs/
*.user

# ZCode session-local tool state
.zcode/
