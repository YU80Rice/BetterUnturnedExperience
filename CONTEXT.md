# 更好的未转变者体验

> 基础设施文件；当前内容由 GPT 撰写。

这是一个公开、开放、支持多创作者协作的《未转变者》原版体验优化插件语境。它以兼容性和规范化扩展为核心，使多个独立功能能够持续汇入同一个插件产品。

## 项目与功能

**更好的未转变者体验**：
承载共享框架和多个原版体验优化功能的整体插件产品。
_避免_：功能合集、整合包

**功能模块**：
由一名或多名创作者独立维护、通过公共契约接入整体插件的单项体验优化能力。
_避免_：子插件、随手补丁

**BUE V2 产品方向**：
在继续提供官方原版体验优化功能的同时，把 BUE 发展为第三方功能可接入、可管理且遵守统一兼容边界的开放运行时平台；平台能力与官方功能使用同一套公开契约。
_避免_：只做功能合集、只做空壳加载器、官方功能私有通道

**官方功能模块**：
由 BUE 官方发行版内置并承担持续维护与资格验证责任的功能模块；其来源可以是 BUE 原生功能，也可以是经迁移和治理裁决后纳入的既有项目能力。
_避免_：拥有框架私有特权的功能、未经裁决直接打包的外部插件

**生态功能模块**：
由第三方作者独立发布、通过 BUE 公开契约接入运行时和统一管理面板，但不随 BUE 官方发行版交付的功能模块。
_避免_：自动视为官方功能、要求并入 BUE 主仓库

**官方纳入**：
把既有独立项目的一项能力迁移为 BUE 官方功能模块，并由 BUE 官方发行版承担其后续维护、兼容和资格责任；它不等于把原项目原样拼入主 DLL。纳入形态是"吃掉并消化"——吸收其能力与语义，以 BUE 自己的实现/契约/生命周期重新表达，原项目不再作为独立运行时保留。
_避免_：简单合并 DLL、复制全局状态、保留两套事实源、把原项目当黑盒外包挂载

**官方纳入后的可选功能**：
已纳入 BUE 官方发行版的功能必须由 BUE 提供，但玩家可以通过 BUE 管理面板关闭；“玩家可以不用”不等于“BUE 可以不提供”。
_避免_：要求玩家重新寻找分散的独立 DLL、把关闭功能等同于卸载功能

**统一功能设置面板**：
BUE 管理面板展示所有已接入功能的可变设置，并提供统一编辑入口；每个功能模块仍拥有自身设置的校验、持久化和运行时权威，BUE 不替代这些事实源。
_避免_：BUE 直接修改私有字段、把所有设置复制成第二事实源

**官方功能成熟度分级**：
官方功能按核心、可选、实验性分级管理；分级决定默认启用和稳定性承诺，不改变功能模块必须遵守的兼容、回退和隔离约束。
_避免_：所有官方功能默认启用、用实验功能绕过资格门禁

**官方纳入后的交付入口**：
当既有项目能力被正式纳入 BUE 后，BUE 成为玩家侧的唯一推荐交付入口；旧项目可以保留历史页面和迁移说明，但不再要求玩家同时下载分散的独立 DLL。
_避免_：BUE 与旧项目长期并行导致重复加载、配置冲突或重复初始化

**Launch 系列功能家族**：
由同一作者（YU80Rice）开发、历史上作为独立 BepInEx 插件发布并依赖 LMN 通信的功能集合（LMN 网络模块、LaunchInventoryTidy、LaunchInPlaceReload、LaunchHordeTracker 等）；按 V2 方向，这些功能不再作为单独插件存在或单独维护，而是并入 BUE 内部作为官方功能（玩家只部署 `BetterUnturnedExperience.dll`）。
_避免_：把 Launch 系列当作外部生态继续要求独立安装、让官方纳入停留在"并列共存"而非"吃掉消化"

**官方功能默认策略**：
核心和可选官方功能默认启用，实验性官方功能默认关闭；所有功能仍允许玩家在 BUE 管理面板中调整。
_避免_：把每个功能的默认值交给无统一规则的临时决定、让实验功能静默改变玩家环境

**功能级故障隔离**：
官方功能发生故障时，BUE 默认只隔离该功能并保留其他功能与原版游戏；只有核心运行时或共享契约损坏时才进入全局核心安全降级。
_避免_：自动重启异常功能、让单个功能故障拖垮整个 BUE

**LMN 官方纳入方向**：
V2 第一项官方纳入能力是 LMN 的多人插件通信能力，用来补足 BUE 当前网络传输层在 U3DS 和本地联机环境中缺少插件交流通道的问题；LMN 将迁移为 BUE 的官方网络模块，而不是要求玩家额外安装独立 LMN DLL。
_避免_：把 LMN 当作普通 UI 功能、用 BUE 的未完善网络层替代 LMN 已解决的通信职责

**BUE 网络 API**：
V2 对新开发者公开的网络规范和传输入口，名称为 `BueNetwork` / `BueNetworkApi`；它提供稳定的频道与传输契约，具体功能模块仍自行定义业务负载、身份授权、限流、幂等和事务规则。
_避免_：让开发者直接依赖 BUE 内部传输实现、把网络 API 当作业务协议或权限系统

**数字频道**：
以 0..255 整数编号寻址的旧频道方式，只保留给既有旧插件的兼容接收/运行，不提供新的注册入口；正式产品语言说"数字频道"。"V1"是内部/历史词汇，不面向玩家与开发者。
_避免_：把迁移阶段版本号当作频道方式本身、在产品面板或开发者材料里使用 V1/V2 内部词汇

**命名频道**：
`BueNetworkApi` 的正式频道方式，频道身份是稳定的 FeatureId 字符串（反查命名），版本协商在 API 内自动完成；正式产品语言说"命名频道"。"V2"是内部/历史词汇，不面向玩家与开发者。
_避免_：把频道身份称作 GUID（它不是 UUID）、让频道身份依赖显示名或文件名

**V1 兼容边界**：
BUE 只为既有 V1 消费方保留兼容接收/运行能力，不再提供新的 V1 注册手段；所有新功能和新插件必须使用 BUE V2 命名频道与 `BueNetworkApi`，旧插件应按迁移文档升级。
_避免_：继续扩大数字频道技术债、强迫旧插件一次性失去兼容性

**网络能力接管**：
当 BUE 与独立 LMN 同时安装时，BUE 负责检测并停用独立 LMN 的运行实例，由 BUE 官方网络模块提供唯一的网络 Hook、频道注册和消息路由；接管必须可诊断、可恢复且不能误伤不相关插件。
_避免_：两个网络实现同时处理同一帧、静默停用用户插件、重复安装原生协议 Hook

**BUE 网络模块默认策略**：
BUE 网络基础模块默认启用；没有网络功能使用时保持低开销，具体官方或生态功能仍可在统一管理面板中独立开关。
_避免_：要求玩家手动启用基础网络才能获得官方功能、把基础模块开关当作服务器授权

**LMN 配置迁移**：
BUE 接管独立 LMN 时自动读取并迁移兼容配置，保留原有有效值；迁移应可回滚并记录结果，不直接破坏旧配置文件。
_避免_：静默丢失用户配置、把迁移失败当作成功

**独立 LMN 文件处理**：
检测到独立 LMN DLL 时，BUE 不删除用户文件，而是阻止其重复运行并向用户说明已由 BUE 接管；用户可在确认后自行清理旧文件。
_避免_：自动删除用户文件、让两个网络实现同时运行

**V1 兼容过渡期**：
BUE 对 V1 只保留明确的过渡兼容能力，不再提供新的 V1 注册入口；过渡期结束后，新架构生态统一使用 V2 命名频道和 `BueNetworkApi`。
_避免_：无限期扩张 V1 数字频道、没有迁移终点

**V2 第一阶段范围**：
V2 第一阶段只完成 LMN 网络模块的官方纳入、接管、配置迁移和 BUE Network API；本地联机、性能优化、背包整理等其他能力暂不在第一阶段同步迁移。
_避免_：在网络基础替换尚未稳定时并行迁移多个高风险领域

**LMN 纳入完成标准**：
LMN 只有在 BUE 内置 `BueNetworkApi`、V2 命名频道可用、V1 过渡兼容仍可运行、独立 LMN 共存接管生效、配置迁移完成，并分别通过单人、U3DS 和 SteamP2PFriends 网络层验证后，才可视为 V2 第一阶段完成；网络模块故障时本地功能仍必须保持可用。
_避免_：只完成 API 搬运就宣称网络模块迁移完成、用单一环境证据替代三环境验证

**网络接管用户提示**：
BUE 与独立 LMN 共存时，在 BUE 管理面板显示“已由 BUE 接管”和配置迁移状态，同时保留必要日志；不弹出阻止游戏启动的强制确认窗口。
_避免_：静默停用、强制阻塞启动、让玩家无法理解独立 LMN 的状态

**V1 兼容退出条件**：
V1 兼容过渡期在主要官方功能和主要生态插件完成 V2 迁移后结束，由实际迁移覆盖率和生态准备程度决定，而不是仅按某个日期或版本号自动结束。
_避免_：无限期保留 V1、在生态尚未准备好时突然移除兼容

**V2 第一阶段迁移资料**：
LMN 纳入 BUE 的第一阶段必须同时提供 V1→V2 API 对照、命名频道示例、配置迁移说明、BUE/LMN 共存接管说明、单人/U3DS/P2P 验证要求以及常见错误与回退行为文档。
_避免_：只交付代码而不给开发者可执行的迁移路径

**玩家侧单 DLL**：
BUE 官方发行版继续以单一 `BetterUnturnedExperience.dll` 交付；仓库内部允许按领域模块化开发并在构建时聚合，第三方生态功能仍可保持独立 DLL。
_避免_：内部模块化等同于玩家安装多个 BUE 核心 DLL、强迫第三方并入主 DLL

**功能声明**：
创作者在仓库中提交的功能身份、版本、依赖、能力、环境目标和入口意图；它是待裁决的贡献输入，不是运行时事实或验收结论。
_避免_：功能定义、运行时描述、已验证清单

**规范功能定义**：
框架把各领域已裁决的功能定义片段链接后形成的唯一声明事实；运行状态、会话状态和验收证据仍由各自事实源维护。
_避免_：原始 feature.json、运行时 Describe 返回值、发布证据

**功能定义编译**：
从领域声明编译、全仓链接到确定性产物组装的管线总称；它不是一个理解所有领域规则的万能模块，其成功不证明 DLL 或运行环境已经验收。
_避免_：Manifest 校验、发布编译、运行时发现

**功能定义片段**：
由身份、关系、设置、能力或表现等领域自行裁决的类型化声明事实，是功能定义链接的输入。
_避免_：原始声明、万能功能 DTO、运行时状态

**链接功能定义包**：
功能定义片段完成跨片段引用、唯一归属和版本闭包后形成的全仓规范功能定义集合；不同领域从中确定性解释自己的消费视图。
_避免_：发布包、运行时状态快照、证据包

**定义产物**：
把链接功能定义包及其静态消费视图按规范格式编码后形成的单一构建输入；它与最终 DLL 配对，但本身不是发布物或验收证据。
_避免_：多个注册表文件、发布包、运行时配置

**资格义务**：
根据规范功能定义和项目政策推导出的静态检查与环境验证要求，描述某个候选产物必须证明什么，但不记录测试结果。
_避免_：环境支持声明、测试结果、发布门禁结论

**证据案例**：
绑定具体候选 DLL、场景、角色和 Case ID 的不可变观察材料集合；新 DLL 身份会使旧案例失去适用性。
_避免_：通用测试报告、功能声明、发布批准

**技术资格裁决**：
把资格义务与同一候选产物的证据案例比较后得到的 Fulfilled、Failed、Missing、Stale 或 NotApplicable 结论。
_避免_：功能运行状态、成熟度、发布授权

**发布授权**：
在技术资格完整且治理审批满足后，允许某个具体候选产物发布或提升成熟度的人工裁决。
_避免_：构建成功、测试通过、自动发布

**更好的物品交互**：
首个功能模块，改善玩家在物品栏与容器中的物品摆放和拖动体验。
_避免_：更好的背包交互

**玩家物品栏网格**：
玩家通过 G 打开的背包网格（U3-SDK 页 2–6：Hands、Backpack、Vest、Shirt、Pants），是“更好的物品交互”受支持来源或目标网格；DEV-16F 起全部玩家网格页均进入增强路径。
_避免_：泛指所有玩家界面

**普通容器网格**：
玩家通过 F 或 G 打开的箱子、车辆后备箱等可摆放物品的网格（U3-SDK 页 7 Storage/trunk，共享视觉 surface）；V1 只增强拖入，不增强拖出。
_避免_：尸体或未裁决的特殊页面（AREA=8 与装备槽页 0/1 保持原生 Pass-Through）

**增强拖入**：
由“更好的物品交互”提供候选计算、占据预览与受控原生提交的从地面或另一受支持网格放入玩家物品栏/普通容器的交互路径；它不拥有库存权威。
_避免_：增强丢弃、平行库存提交

**增强交互开关**：
玩家控制“更好的物品交互”是否接管受支持网格的预览与拖入体验的本地偏好；关闭时恢复原生拖拽，之后可再次开启。
_避免_：服务器授权开关、永久禁用

**自动旋转**：
“更好的物品交互”在当前方向局部或全局无法放置时，是否允许尝试原生顺时针 90° 方向的玩家本地体验偏好。朝向切换由**几何空间与边缘引力驱动**，而非物品历史方向记忆驱动：物品拿起时以当前姿态进入开阔区域，开阔区域保持当前方向（防蠕动），仅当光标进入边缘感应带或因当前方向确实放不下时才切换朝向。
_避免_：自动交换、自动整理、全局跨生命周期方向记忆

**边缘感应带（Edge Sensing Band）**：
容器或空位边界附近、以光标网格坐标为基准的触发区域。竖向感应带（左/右壁）带宽由 `containerWidth` 计算，横向感应带（上/下壁）带宽由 `containerHeight` 计算，统一公式 `band(dim) = clamp(1.0, dim * 0.15, 2.0)`（格）。光标落入带内即产生对应方向的贴边倾向。
_避免_：单列/单行硬边界触发、按历史方向记忆触发

**边缘引力（Edge Gravity）**：
光标处于边缘感应带内时，自动旋转倾向使物品长边顺着该边缘方向贴边（竖向带 → 竖长边贴左/右壁；横向带 → 横长边贴上/下壁）。该倾向受**物理容纳守卫**约束：仅当旋转方向 footprint 在当前区域确实放得下（`rotatedFitsGrid && Fits`）时才触发，放不下绝不盲目翻转。障碍整行/整列挡出的空位边界与容器外壁等价，同样产生贴边引力（允许前序物品充当“人造侧壁”实现并排收纳）。
_避免_：无容纳守卫的盲目旋转、仅容器外壁触发

**开阔中部保持方向（D2 防蠕动）**：
光标远离任何边缘感应带（开阔中部，如 6×3 网格的 (2,1) 或大背包正中央）时，当前方向能放下即保持当前方向，不与旋转方向竞争，避免横竖“蠕动”。角落重叠区（同时处于竖向带与横向带内）同样保持进入时的当前姿态，重叠区外由感应带平滑接管。
_避免_：双方向无差别距离竞争、开阔中部强制自愈翻转

**原生回退**：
增强功能关闭、不可用、冲突或隔离时，保留 Unturned 原生拖拽和库存行为的用户可见状态。
_避免_：伪造回滚、静默修改原生权威

**Better Item Interaction FeatureId**：
官方功能模块的不可变运行时身份：`io.github.yu80rice.bue.better-item-interaction`；它与 BUE 主插件身份及显示名称分离。
_避免_：旧版 `io.github.yu80rice.betterunturned.iteminteraction`、显示名称充当身份

**BUE 管理面板**：由 BUE 单一运行时 DLL 内置的插件与功能设置管理界面；其交互方式参考并吸收 `35117/UnturnedPluginManager` 的公开实现经验，但运行时不依赖外部 PluginManager DLL。
_避免_：外置管理器硬依赖、第二个 BUE 管理 DLL

**实现来源署名**：BUE 仓库对吸收的第三方实现保留原作者、来源仓库、版本/提交和许可证说明，并在贡献文档中明确致谢；署名不等于运行时程序集依赖。
_避免_：无来源复制、把致谢误作 ABI 依赖

**管理面板收藏排序**：玩家对管理条目进行收藏后，收藏条目始终排在未收藏条目前；收藏条目内部按玩家加入收藏的先后顺序排列，未收藏条目再按玩家选择的 A→Z 或 Z→A 排序。
_避免_：按显示名称推断收藏身份、每次刷新随机改变收藏顺序

**管理条目身份**：BUE 管理面板使用稳定的 BepInEx GUID 或 BUE FeatureId 作为条目身份，不使用显示名称、程序集文件名或列表位置作为持久化键。
_避免_：改名导致收藏丢失、同名插件互相覆盖

**官方功能显示名**：
官方功能在管理面板呈现给玩家的固定中文名——更好的物品交互、背包整理、更好的换弹体验、更好的尸潮播报；显示名不是身份，条目身份仍由 FeatureId 或插件 GUID 承担。
_避免_：用显示名做持久化身份、给外部插件代改显示名

**管理面板排序偏好**：玩家可选择未收藏条目的 A→Z 或 Z→A 排序；收藏条目始终优先，并按玩家最近一次加入收藏的顺序排列。取消收藏后再次收藏会进入收藏序列末尾。
_避免_：按刷新时间排序、取消收藏后隐式保留旧位置

**BUE 兼容配置编辑**：对未接入 BUE Settings Facet 的普通 BepInEx 插件，BUE 仅通过受限的 `ConfigEntry` 兼容适配器编辑明确支持的基础类型（布尔、数字、字符串）；不支持类型只读显示，危险路径、任意代码和任意文件操作不属于该能力。
_避免_：把任意配置对象当作可执行扩展点、绕过插件自身校验

**BUE 设置编辑契约**：需要事务语义、复杂校验、服务器策略或 UI 能力的功能，应注册 BUE Settings Facet；普通 ConfigEntry 兼容编辑不替代该契约。
_避免_：第三方功能依赖 BUE 反射私有字段、把 ConfigEntry 当作完整功能协议

## 协作边界

**前端**：
玩家能够看到或直接操作的游戏内 UI、HUD 与交互反馈层。
_避免_：Web 前端、规则层

**后端**：
支撑插件及功能模块运行、但不直接呈现给玩家的规则、状态、存储、权限与同步层。
_避免_：独立 Web 服务

**共享契约**：
前端、后端及功能模块之间共同遵守的协议、数据类型、事件名、错误码与版本兼容规则；其维护责任归 GPT。
_避免_：内部实现接口、临时数据结构

**设置描述**：
功能模块注册的设置身份、类型、默认值、权威、可见性和校验规则；它描述可配置能力，但不持有当前有效值。
_避免_：UI 控件、配置文件行

**设置快照**：
某一功能在特定作用域和 revision 下的完整只读有效值与编辑权限投影，是前端显示和回滚的最小一致性单位。
_避免_：单字段回调、可变设置字典

**会话权威覆盖**：
客户端连接多人服务器期间消费的服务器设置快照；它只决定当前连接的有效值，断线即清除且不覆盖客户端持久化偏好。
_避免_：服务器配置下载、永久同步

**Unturned 官方（SDG）**：
《未转变者》游戏本体及其原生接口与运行时（`ITransportConnection`、`IClientTransport`、`SDG.NetTransport`、`NetMessages`、SteamNetworkingSockets 等）的所属方；它是 BUE 与所有功能模块只能消费、不能拥有的地基。
_避免_：把原生接口能力当作 BUE 自有、修改或重定义 Unturned 原生语义

**BUE 官方**：
由 BUE 发行版内置并承担维护、兼容与资格责任的功能模块与平台能力（BII、管理面板、BueNetworkApi、未来的 LMN 网络模块等）的所属方；它决定什么进入官方发行版、按什么契约表达。
_避免_：把第三方功能未经验证地称为 BUE 官方、把 BUE 官方能力与 Unturned 原生能力混为一谈

**客户端间第三方通信平台**：
Unturned 官方与 BepInEx 官方都没有提供的、供第三方插件在客户端之间直接交流消息的标准化平台（类似 Minecraft Forge 的 mod 间网络能力）；BUE 作为 Forge-like 功能平台，通过纳入 LMN 并提供 BueNetworkApi 来补足这个缺口。
_避免_：把原版协议包当作插件通信通道、要求玩家额外安装独立网络 DLL 才能获得官方功能

**频道协商**：
LaunchMultiplayerNet 对双方是否接受某个命名频道及频道版本的传输层确认；它不证明功能兼容，也不授予权限。
_避免_：插件握手、身份认证

**能力协商**：
框架在频道可用后交换契约、功能和能力清单，并由服务器为当前连接裁定可用交集的应用层过程。
_避免_：客户端自报授权、版本字符串比较

**连接代际**：
一次具体连接与应用握手的本地单调身份；重连、换服或 transport identity 改变会创建新代际并使旧消息、快照和缓存失效。
_避免_：SteamID、认证令牌

**注册阶段**：
BUE Host 从启动到运行的就绪状态机（HostStarting → RegistrationOpen → CatalogFrozen → RuntimeReady），只有 RegistrationOpen 接受功能注册，之后一律拒绝并返回稳定原因码。
_避免_：任意时刻注册、按插件到达顺序决定身份

**CatalogRevision**：
全部功能注册完成并规范化后，按 FeatureId/digest 确定性排序生成的目录修订；它取代"谁先注册谁优先"的到达顺序，作为对外投影的稳定身份事实。
_避免_：插件到达顺序、运行时增量修订

**LoadSetIdentity**：
BUE Host、已装功能 DLL、ClientUi satellite、Definition Artifact 与 reference-set 的身份与哈希按稳定序序列化后的 canonical 摘要；证据案例必须绑定完全相同的 LoadSetIdentity，单个 DLL 哈希不能代表完整部署环境。
_避免_：单一 BUE DLL 哈希冒充整体环境、路径/顺序相关的可变身份

**功能表现状态**：
核心功能成功但 ClientUi satellite 缺失或失败时的只读表现投影（Available / PresentationDegraded / HeadlessOnly / Failed），与功能运行状态分离；表现降级不影响 Settings Facet 可用性。
_避免_：把表现状态混入功能状态、缺 UI 等于功能失败

**功能隔离**：
单个功能发生未处理异常后，核心停止其新回调、撤销登记资源并保持其他功能与原版游戏继续运行的局部降级状态。
_避免_：插件整体崩溃、自动重启循环

**核心安全降级**：
Contracts 或核心运行时不变量损坏时，框架尝试停止全部自定义功能并保留最小诊断，使原版游戏尽可能继续运行的不可恢复状态；清理可能部分失败。
_避免_：功能隔离、假装正常运行

## 运行环境

**单人环境**：
游戏逻辑与玩家交互都在本地进程内运行的受支持场景。
_避免_：离线特供实现

**SteamP2PFriends 环境**：
由 SteamP2PFriends 提供本地联机能力的受支持多人场景。
_避免_：原生 Listen Host

**U3DS 环境**：
由 Unturned Dedicated Server 承载权威游戏状态的受支持多人场景。
_避免_：P2P 主机
